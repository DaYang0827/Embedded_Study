进制转换表

|       概念换算        |        关系         |            嵌入式实战联想            |
| :---------------: | :---------------: | :---------------------------: |
|  **1 Byte (字节)**  | 8 Bit (比特)，两个十六进制 |          寄存器里最小的配置单位          |
| **1 Word (32位字)** |  4 Byte = 32 Bit  |   **PC 和 SP 寄存器**里存储一个地址的大小   |
|   **十六进制 1 位**    |      二进制 4 位      |        `0xF = 0b1111`         |
|   `**十六进制 2 位**   | **1 个字节 (Byte)**  |    `0xFF` = 1个字节的最大值 (255)    |
|   **十六进制 8 位**    |  1 个 32位字 (Word)  | `0x080012A4` = 一个标准的 ARM 代码地址 |

单位变化表

| 单位     |     全称     | 含义        | 进制      | 常见应用场景              |
| :----: | :--------: | :-------: | :-----: | :-----------------: |
| **b**  |   `bit`    | 1个二进制位    | ——      | 计算机最底层数据            |
| **B**  |   `Byte`   | 1个字节 (8位) | 1B = 8b | 文本字符、基础计算           |
| **kb** | `kilobit`  | 1千比特      | 1000进位  | 电话语音、音频码率           |
| **KB** | `KiloByte` | 1千字节      | 1024进位  | 纯文本文件、相片缩略图         |
| **Mb** | `Megabit`  | 1兆比特      | 1000进位  | **宽带网速**、4K视频流      |
| **MB** | `MegaByte` | 1兆字节      | 1024进位  | **手机内存**、手机拍照、MP3音乐 |

# 1 处理器内核与架构基础

## 1.1 数据存储与编址

### 1.1.1 存储器

在嵌入式系统和计算机架构中，存储器主要分为两大阵营：**RAM（随机存取存储器，掉电丢失）** 和 **ROM/Flash（只读/闪存，掉电不丢失）**

1. 存储器的两大核心分类

|              存储器大类               |            常见类型            |            特点             |                   在嵌入式系统中的典型用途                   |
| :------------------------------: | :------------------------: | :-----------------------: | :----------------------------------------------: |
|     **RAM**  <br>(运行内存，速度极快)     |      **SRAM** (静态RAM)      |      芯片内部，无需刷新，速度最快。      | 存放 **FreeRTOS 的任务堆栈**、**LVGL 的显存缓冲区（VDB）**、全局变量。 |
|                                  |  **DRAM / SDRAM**（动态RAM）   |      芯片外部扩展，容量大，需刷新。      |      在跑大型 GUI（如高分辨率 LVGL）时，用作外部大显存或扩展运行内存。       |
| **ROM / Flash**  <br>(程序存储，掉电不丢) |  **Nor Flash** (如内部Flash)  | 支持芯片内执行（XIP），读取速度快，按字节读取。 |     存放 **可执行代码（Code）**、**LVGL 的静态图片/字库常量**。      |
|                                  | **Nand Flash** (如SD卡/eMMC) |     容量极大，擦写寿命长，按块读写。      |      存放大容量文件（如 UI 音视频素材、**OTA 升级固件的暂存区**）。       |
在早期的电子产品中，程序一旦写死在 ROM 里就再也无法更改。如果要修复一个 Bug，必须把整个芯片拆下来扔掉，换一块新的芯片，这在现代电子产品（尤其是需要 **OTA 远程升级** 的产品）中是不可接受的

关于ROM是有完整的进化史的
- **真正的` ROM`（Mask ROM，掩膜只读存储器）**：在芯片出厂制造时，程序就通过光刻机“焊死”在电路里了。**完全无法修改**，只能读。
- **PROM / EPROM**：虽然可以写，但极其麻烦。PROM 只能写一次（写错芯片就废了）；EPROM 需要用**紫外线灯**照着玻璃窗口照射几十分钟才能擦除，无法在电路板上直接操作。
- **FLASH（闪存）**：可以在普通的电信号控制下，**在电路板上直接进行单字节或按块的擦除与写入（电可擦除）**。

---

2. 实例讲解

   RAM（运行内存）里都在跑什么？

   当系统运行时，FreeRTOS 和 LVGL 会疯狂消耗 RAM：

- **FreeRTOS 堆栈（Heap/Stack）**：每个你创建的 FreeRTOS 任务（Task），都需要在 SRAM 中开辟一块空间存放它的上下文。
- **LVGL 缓冲区（Draw Buffer）**：LVGL 刷新屏幕时，需要先在 SRAM 中把画面“画”好，再发送给 LCD。分辨率越高，消耗的 RAM 越恐怖。

  Flash（程序存储）里都存了什么？

芯片烧录后，固件是静态存放在 Flash 里的：

- **代码区（RO-Code）**：FreeRTOS 的调度算法、LVGL 的控制逻辑代码。
- **只读数据区（RO-Data）**：你在 LVGL 中用到的 **图标图片、艺术字库**。它们非常占空间，通常直接固化在 Flash 中，用的时候才读取。

OTA 时的存储器是如何工作的？

OTA 的本质，就是**对 Flash 进行擦除和重新写入**的过程：

-  **下载阶段**：4G/Wi-Fi 模组收到新的固件包，由于数据量大，RAM 装不下，必须一边下载，一边写入 **Flash 的“备份区（Slot 1）”**。
-  **校验阶段**：FreeRTOS 调度算法从 Flash 备份区读取数据，在 RAM 中计算 CRC/MD5 校验码。
-  **重启阶段**：Bootloader 运行，把备份区（Slot 1）的新固件，搬移覆盖到**运行区（Slot 0）**，完成升级。

---

3. 常见术语：RAM 的三大段 (Heap, Stack, Static)

在看编译报告（如 Keil 的 `.map` 文件）时，你常会看到这些专业词汇，它们统统属于 RAM：

- **Stack（栈）**：局部变量、函数跳转时的现场保护。在 FreeRTOS 中，每个任务都有独立的 Stack。
- **Heap（堆）**：动态内存分配。例如用 `malloc()` 或 FreeRTOS 的 `pvPortMalloc()` 申请的空间。**LVGL 创建动态小部件（如弹窗）时极度依赖 Heap**。
- **Static/Global（静态/全局区）**：你在代码最外层定义的全局变量。

**栈（Stack）和堆（Heap）都属于 RAM**，但它们在**内存管理方式**、**数据生命周期**和**使用场景**上有本质的区别。

如果把 RAM 比作一家**高档餐厅**：

- **栈是“旋转火锅”**：座位固定，菜品按顺序转过来，吃完自动收走。速度极快，但你不能挑选座位的长短。
- **堆是“自选包厢”**：需要服务员（`malloc`）根据你报的人数动态开辟空间，吃完必须主动退房（`free`）。空间很大，但手续繁琐。

| 维度       | 栈 (Stack)              | 堆 (Heap)                        |
| :---: | :---: | :---: |
| **分配方式** | **系统自动分配**和释放。         | **程序员手动申请**和释放。                 |
| **申请语法** | C语言中直接定义：`int a = 10;` | C语言中动态申请：`malloc(100);`         |
| **空间大小** | **非常小**。单片机中通常只有几 KB。  | **非常大**。可以占满除去栈和全局变量后剩下的所有 RAM。 |
| **管理结构** | 先进后出（FILO），像一叠盘子。      | 链表/内存块管理，像一堆凌乱的储物柜。             |
| **分配速度** | 极快。CPU 专门有指令寄存器（SP）控制。 | 较慢。需要在内存中寻找足够大的空闲碎片。            |
| **释放风险** | 无风险。函数执行完，系统自动回收。      | 高风险。不手动释放会导致**内存泄漏**。           |

### 1.1.2 统一编址与内存映射

在嵌入式开发中，芯片内的世界被称为 **“统一编址空间”**。CPU 就像规划大都市一样，把从 `0x00000000` 到 `0xFFFFFFFF` 的 4GB 土地，严格划分成了不同的“特区”。

|    基地址 (起跑线)     |      所属物理设备      |           里面装的到底是什么？            |                     软件层面的“常驻居民”                     |
| :--------------: | :--------------: | :-----------------------------: | :-------------------------------------------------: |
| 🏁 `0x00000000`  |    Boot/复位向量区    |   芯片刚上电那一纳秒，CPU 强制去这里读取开机配置。    |          开机时的 **SP 栈顶初始值**与 **PC 启动首地址**。           |
| 🎬 `0x08000000`  | 片上 `FLASH (ROM)` |      “静态剧本”。 通电后只能读，不能随便改写      | 编译后的 **ARM 机器指令 (Code)**、**LVGL 图片/字库常量 (RO-Data)** |
| 🧠 `0x20000000`  | 片上 `SRAM (RAM)`  |    “动态草稿纸”。速度极快，随时高频读写，断电全丢     |      你的**普通的全局变量**、**堆 (Heap)**、**栈 (Stack)**.      |
| 🛠️ `0x40000000` |     片上外设寄存器区     | 芯片的“物理控制台”。读写这里就是直接改变底层硬件引脚的状态。 |    **GPIO 控制寄存器**、**SPI/UART 寄存器**、**DMA配置寄存器**。    |

```text
▲ 高地址 0xFFFFFFFF (4GB 顶端)
│
├─────────────────────────────────────────────────┐
│   Cortex-M 内部私有外设区 (Private Peripherals)   │ 
│   (0xE0000000 - 0xE00FFFFF)                     │ ◄── 内核自带的 NVIC中断控制器、   │                                                 │            SysTick定时器
├─────────────────────────────────────────────────┤
│   保留区 / 芯片商自定区                           │
├─────────────────────────────────────────────────┤
│   外部外设 / FMC 扩展存储区                       │
│   (0x60000000 - 0x9FFFFFFF)                     │ ◄── 用于外接外部 RAM、LCD 屏等
├─────────────────────────────────────────────────┤
│   片上外设寄存器区 (Peripherals)                  │
│   (0x40000000 - 0x5FFFFFFF)                     │ ◄── 0x40000000 就在这里（硬件控  │                                                 │                制开关）
├─────────────────────────────────────────────────┤
│   片上内置 SRAM 区 (主内存)                       │
│   (0x20000000 - 0x3FFFFFFF)                     │ ◄──  .data, .bss, Heap,        │                                                 │           Stack 都在这里
├─────────────────────────────────────────────────┤
│   片上内置 Flash 区(代码存储区)                    │
│   (0x08000000 - 0x1FFFFFFF)                     │ ◄── 重点！Bootloader 和 APP 的  │                                                 │              家就在这里
├─────────────────────────────────────────────────┤
│   别名/引导区 (Aliased to Flash/RAM/ROM)         │
│   (0x00000000 - 0x07FFFFFF)                     │ ◄── 启动映射区（包含芯片出厂自带的 │                                                 │               Boot ROM）
└─────────────────────────────────────────────────┘
▼ 低地址 0x00000000 (0GB 底端)
```


**PC 寄存器是 CPU 盒子里装的“当前代码进度条”**。当单片机按下复位键或者刚刚通电的那一刹那，硬件电路会自动进行以下惊天大动作：

1. **第一步（找 SP 初始值）**：CPU 的硬件会自动去 FLASH 的大门口 —— 也就是 **`0x08000000`** 这个房间里，读取里面存放的 32 位数字。这个数字记录了**栈底指针（SP）** 应该从 RAM 的哪里开始向下生长。
2. **第二步（找 PC 初始值）**：紧接着，CPU 去隔壁的 **`0x08000004`** 房间读取第二份数据。这个房间里存着的，是整个固件启动代码（如 `Reset_Handler`）的第一行 C 语言代码编译出来的机器码地址。
3. **第三步（程序跑起来）**：CPU 把读取到的这个启动地址直接塞进 **PC 寄存器** 盒子里。PC 一拿到这个地址，指挥棒一挥，整个芯片的代码（包含后面的 `main()` 函数和 FreeRTOS 调度）就此轰轰烈烈地运转起来了！

这两个最核心的开机数据房间（`0x08000000` 和 `0x08000004`），在嵌入式专业术语里叫做  **“中断向量表（Vector Table）”** 的开头。

### 1.1.3 地址排序与数据存储

**地址以 Byte 为单位连续排列**

在芯片的物理世界里，**每一个唯一的地址都代表 1 个字节（8比特，两个十六进制）的空间**。  
所以，内存里其实像居民楼一样，房间号是连续挨着的：

- `0x00000000`：第 0 号房间（装 1 字节数据）
- `0x00000001`：第 1 号房间（装 1 字节数据）
- `0x00000002`：第 2 号房间（装 1 字节数据）
- `0x00000003`：第 3 号房间（装 1 字节数据）

这四个紧挨着的房间加起来，正好是 **4 个字节**（也就是 32 比特，等于 1 个**字**）。

0x08000000 = 存放“SP初始值”的Flash地址

0x08000004 = Vector Table 中存放 Reset Handler 地址的那个 Flash 单元

- **内存对齐铁律**：32位字最好存放在能被4整除的地址上（如 `0x00`, `0x04`）对齐访问通常性能最好，也有些指令和内存区域要求严格对齐；部分 Cortex-M3/M4 普通非对齐访问可以由硬件支持，但某些设置、指令或内存类型仍可能导致异常。
- **跨界无意义**：像 `0x01~0x04` 这种跨越数据边界的拼接，数据逻辑上是乱码，硬件上会引发死机。

在专业的地址映射图中，**数字越小、地址越低，就越被画在最下面**，0x00000000是地基，在最下面

```text
【 顶 天 ── 4G 空间的尽头：0xFFFFFFFF 】
  │
  │   [ 各种高级外设和内核寄存器占用的高处领地 ]
  │
  ├── 0x40000000 ── 🛠️ 外设寄存器区起点（控制引脚、时序）
  │
  ├── 0x20002000 ── 🏁 栈底（开机初始 SP 位置，在 RAM 顶部悬挂）
  │    ▼ 栈向下长（地址减小）
  │    ▲ 堆向上长（地址增大）
  │   0x20000000 ── 🧠 RAM 运行内存起点（全局变量、堆栈双向奔赴）
  │
  ├── 0x08000000 ── 🎬 FLASH 静态剧本区起点（存放机器指令、常量）
  │
【 地 基 ── 统一编址总起点：0x00000000 】 ➡️ 映射了 FLASH 前几个房间
```

**堆和栈都在物理 RAM（运行内存）** 它们的具体起点和终点并不是固定的，而是由编译器在编译时，根据你的芯片 RAM 大小以及全局变量的多少来精准划分

在标准的 32 位嵌入式系统（如 STM32）中，它们的具体起始位置如下：

1. 堆（Heap）的起点：紧跟在全局变量后面

- **位置**：假设 `.data/.bss` 占用到 0x20000400 附近， Heap 通常位于其后，并按照链接器要求对齐。
- **规律**：它永远从 RAM 的低地址端开始，**向上生长（从小到大）**。每当调用 `malloc()` 或 FreeRTOS 创建动态组件时，堆指针的数字就会不断变大。

2. 栈（Stack）的起点：RAM 的最高安全边界（顶部）

- **位置**：假设你的芯片 RAM 总共有 20KB（最高地址到 `0x20005000`），编译器就会把**栈的起点（也就是开机时的初始 SP）设在 `0x20005000`**。
- **规律**：它永远从 RAM 的最高地址端开始，**向下生长（从大到小）**。每当发生函数调用、局部变量入栈，栈指针（SP）的数字就会不断减小。

整体RAM的地图

```text
【 顶 天 ── 芯片物理 RAM 的最高上限边界 】 (例如 20KB 内存的尽头：0x20005000)
  │
  ├── 🏁 栈底（开机初始 SP 寄存器指向的位置） ─── 🚀 栈（Stack）从天花板起跑！
  │    │
  │    ▼ 栈的物理生长方向：【向下生长】（SP 指针随着函数调用、压栈，地址数字越来越小）
  │
  │     [ . . . 🏃‍♂️ 中间这一片庞大的空地，是它们俩共享的“公用动态荒地” 🏃‍♂️ . . . ]
  │
  │    ▲ 堆的物理生长方向：【向上生长】（随着你 malloc()，堆地址数字越来越大）
  │    │
  ├── 🏁 堆（Heap）的起点！ ─── 🎬 堆从地基（.bss 的屁股后面）起跑！
  │
  ├── 🏠 .bss 段 (毛坯房全局变量)
  ├── 🏠 .data 段 (精装房全局变量)
  │
【 地 基 ── RAM 统一编址总起点：0x20000000 】
```
### 1.1.4 内存对齐

内存对齐是指变量地址要按照一定边界存放。

例如：

```c
uint32_t x;
```

`uint32_t` 是 4 字节变量，通常希望它的地址是 4 的倍数：

```text
0x20000000  正确，4 字节对齐
0x20000001  不对齐
0x20000002  不对齐
0x20000004  正确，4 字节对齐
```

---
1. 为什么需要内存对齐

   主要原因：

- CPU 访问更快
- 某些架构不支持非对齐访问
- 避免硬件异常
- 满足外设、DMA、总线访问要求

例如 ARM Cortex-M 中，访问未对齐的 `uint32_t` 可能会变慢，严重时可能触发 `HardFault`。

---

2. 结构体对齐
```c
struct Test {
    char a;
    int b;
    char c;
};
```

看起来大小是：

```text
char a = 1 字节
int b  = 4 字节
char c = 1 字节
总共 6 字节
```

但实际可能是 12 字节，因为编译器会插入 padding。内存布局可能是：

```text
a       1 字节
padding 3 字节
b       4 字节
c       1 字节
padding 3 字节
```

所以：

```c
sizeof(struct Test)
```

可能等于 `12`。

---

3. 优化结构体大小

差的写法：

```c
struct A {
    char a;
    int b;
    char c;
};
```

更好的写法：

```c
struct A {
    int b;
    char a;
    char c;
};
```

这样可以减少 padding。

### 1.1.5 分析内存占用

在 Keil 或 STM32 工程中，经常能看到类似信息：

```c
Program Size: Code=12000 RO-data=800 RW-data=300 ZI-data=5000
```

这些字段反映了程序最终占用了多少 Flash 和 SRAM

1. Code：程序机器码

`Code` 表示程序编译后生成的机器指令，通常存放在 Flash 中。

例如：

```c
void led_on(void)
{
    GPIO_SetBits(GPIOA, GPIO_Pin_5);
}
```

这段 C 代码会被编译器翻译成 ARM 机器指令，这些指令最终存放在 Flash 中。

特点：

```text
存放位置：Flash
内容：CPU真正执行的机器码
是否掉电丢失：不会
是否运行时修改：通常不会
```
---

2. RO-data：只读数据

   `RO-data` 是 Read Only Data，只读数据。

   常见内容包括：

```c
const uint8_t table[] = {1, 2, 3, 4};

const char message[] = "Hello STM32";
```

这些数据通常存放在 Flash 中，因为它们运行时不需要被修改。

需要注意：

```c
const char *str = "Hello";
```

这里要分开看：

```text
"Hello" 字符串字面量：通常在只读区
str 指针变量本身：取决于它定义在哪里
```

如果写在函数内部：

```c
void test(void)
{
    const char *str = "Hello";
}
```

`str` 这个指针变量本身通常在栈上，而 `"Hello"` 字符串内容通常在只读区。

如果写成全局变量：

```c
const char *str = "Hello";
```

指针变量 `str` 本身可能占用 RW-data 或 RO-data，具体取决于修饰方式和编译器安排。

---
3. RW-data：已初始化的全局变量和静态变量

`RW-data` 是 Read Write Data，表示有初始值、运行时可以修改的全局变量或静态变量。

例如：

```c
int global_value = 10;

static uint8_t mode = 1;
```

这些变量运行时需要放在 SRAM 中，因为程序可能会修改它们。但是它们的初始值又必须保存在 Flash 里，否则单片机断电后就不知道开机时应该初始化成什么值。所以 RW-data 有一个很重要的特点：

```text
初始值存放在 Flash
运行时变量实体在 SRAM
启动时由启动文件从 Flash 复制到 SRAM
```

例如：

```c
int global_value = 10;
```

启动时大致发生：

```txet
Flash 中保存 10
        ↓
Reset_Handler 启动时复制
        ↓
SRAM 中的 global_value = 10
```

之后程序修改：

```c
global_value = 20;
```

修改的是 SRAM 中的运行时变量，不会改 Flash 里的初始值。

---

4. ZI-data：未初始化或初始化为0的全局/静态变

`ZI-data` 是 Zero Initialized Data，表示启动时被清零的变量。

例如：

```c
int global_count;

static uint8_t rx_buffer[128];

uint32_t flag = 0;
```

这些变量都属于运行时需要放在 SRAM 中的数据。

特点：

```text
存放位置：SRAM
初始值：启动时清零
是否占用 Flash：通常不占用实际初始化数据空间
```

例如：

```c
uint8_t buffer[1024];
```

如果它是全局变量且没有显式初始化：

```c
uint8_t buffer[1024];
```

它通常属于 ZI-data。

启动时，启动代码会把这块 SRAM 清成 0。

如果写成：

```c
uint8_t buffer[1024] = {1};
```

那么它就不再是纯 ZI-data，而会涉及 RW-data，因为初始值不是全0。

---

5. Flash 和 SRAM 的实际占用计算
在 Keil 中通常可以这样理解：

```text
Flash 占用 ≈ Code + RO-data + RW-data
SRAM  占用 ≈ RW-data + ZI-data + Stack + Heap
```

为什么 Flash 中也要包含 RW-data？因为 RW-data 的初始值要保存在 Flash 中，开机后再复制到 SRAM。

例如：

```text
Code     = 12000
RO-data  = 800
RW-data  = 300
ZI-data  = 5000
```

大致表示：

```text
Flash 至少占用：12000 + 800 + 300 = 13100 字节
SRAM 至少占用 ：300 + 5000 + Stack + Heap
```

其中 Stack 和 Heap 的大小通常由启动文件、链接脚本或工程配置决定，不一定直接显示在 `RW-data + ZI-data` 中。

### 1.1.6 Endianness（大小端）

 **一个内存地址只能存 1 Byte。** 假设现在有一个 32 位整数：

```c
uint32_t data = 0x12345678;
```

它一共：

```text
32 bit= 4 Byte
```

拆开以后就是：

```c
0x12  0x34  0x56  0x78
```

所以它必须占用 4 个连续的内存地址。假设从：`0x20000000`开始保存。问题来了：`0x12` 应该放在低地址，还是 `0x78` 应该放在低地址？这就是： **大小端 Endianness 要解决的问题。**

1. 大端 Big Endian

**大端：高位字节放在低地址。**

还是：

```c
data = 0x12345678
```

其中：

```text
最高有效字节 = 0x12
最低有效字节 = 0x78
```

大端存储：

```text
低地址
   ↓

0x20000000    0x12
0x20000001    0x34
0x20000002    0x56
0x20000003    0x78

   ↓
高地址
```

所以大端非常符合人看数字的习惯：

```
地址从小到大：

12 34 56 78
```

和原来的：

```
0x12345678
```

顺序一样。

因此可以记： **Big Endian：大的一头先放** 这里的“大”指的是： **高位字节。**

----

2. 小端 Little Endian

小端正好相反：

> **低位字节放在低地址。**

还是：

```
0x12345678
```

小端存储：

```
低地址
   ↓

0x20000000    0x78
0x20000001    0x56
0x20000002    0x34
0x20000003    0x12

   ↓
高地址
```

所以从低地址往高地址看：

```
78 56 34 12
```

看起来和原来的数字反过来了。

可以记：**Little Endian：小的一头先放** 这里的“小”就是： **低位字节。**

### 1.1.7 内存管理

#### 1.1.7.1 Memory Leak（内存泄漏）


## 1.2 芯片启动与初始化

```text
   芯片上电 / Reset 
          ↓ 
    Vector Table[0]
          ↓ 
设置 MSP “栈从哪里开始”
          ↓ 
    Vector Table[1] 
          ↓ 
    Reset_Handler 
          ↓ 
 初始化 .data / .bss 
          ↓ 
      SystemInit 
          ↓ 
         main 
          ↓ 
      正常运行程序 
           │ 
 ┌─────────┴─────────┐ 
 │                   │ 
没有中断          发生中断 
 │                   │ 
继续运行 main    NVIC 查 Vector Table
                         ↓ 
                    对应 Handler 
                         ↓ 
                     执行完返回 
                         ↓ 
                   继续原来的程序
```

1. 芯片复位

   STM32 在以下情况下都可能发生复位：

```
上电
硬件复位
看门狗复位
软件复位
```

   一旦发生复位，CPU 当前正在执行的程序会停止，并重新开始整个启动过程。

   因此：

```
Reset
  ↓
重新加载启动信息
  ↓
重新执行启动代码
```

---

2. 读取初始 SP

   Cortex-M 内核复位后，**CPU 会是雷打不动地去 `0x00000000` 读数据的；但芯片硬件在开机的一刹那，偷偷把 `0x08000000` 处的物理 FLASH“映射（投影）”到了 `0x00000000` 的位置！** 

   这在嵌入式里叫 **“地址映射 / 内存重映射（Memory Remap）”** 。可以用 **“影子戏（或者是量子传送门）”** 的比喻来彻底解释这个硬件魔术：

> 硬件上的“量子传送门”机制

- **CPU 的本能**：ARM CPU 是个瞎子，它只认死理。开机通电那一纳秒，它的硬件电路固定只会把手伸向 `0x00000000` 房间去摸 存放“SP 初始值”的 Flash 地址
- **芯片厂商（ST公司）的魔术**：但是，`0x00000000` 在芯片内部只是一块虚无的“影子地址”（它后面没有连着真正的存储颗粒）。如果 CPU 直接去摸，只能摸到一堆空气（垃圾数据）然后直接死机。
- **开机时的偷梁换柱**：当芯片检测到你的 **BOOT0 引脚接了低电平（默认从主 FLASH 启动）** 时，内部的硬件电路会瞬间合上一个电子开关，**直接把 `0x08000000`（物理 FLASH 的大门口）的画面，1:1 投影（镜像）到了 `0x00000000` 上！**

  此时，对于 CPU 来说：

- 它以为自己访问的是 `0x00000000`。
- 但实际上，因为硬件“量子传送门”的存在，它读取到的**正是物理 FLASH 芯片里 `0x08000000` 处的真数据**。

> 为什么不直接让 CPU 去 `0x08000000` 启动？

   为什么 ARM 官方不把 CPU 开机初始地址直接设计成 `0x08000000` 呢？  **为了灵活性。**

   ARM 是一个通用的 CPU 内核，它卖给 ST 公司做 STM32，卖给别的公司做其他芯片。ARM 规定 CPU 必须从 `0x00000000` 启动，剩下的交给芯片厂商去发挥：

- **如果想从主 Flash 启动（普通运行模式）**：厂商就把 `0x08000000` 映射到零地址。
- **如果想从系统内置的串口 Bootloader 启动（刷机模式）**：厂商就把一出厂就写好的系统 ROM（地址一般是 `0x1FFFxxxx`）映射到零地址。
- **如果想从 SRAM 启动（高速调试模式）**：厂商就把 `0x20000000` 映射到零地址。

  不管厂商后面怎么换物理芯片，CPU 永远只需要闭着眼睛去 `0x00000000` 领任务就行了

---

3. 读取 `Reset_Handler`

   紧接着 CPU 读取向量表中的第二个 32 位数据：

```
0x08000004
```

   这里存放：

> **`Reset_Handler` 的入口地址。**
 
  例如：

```
Flash 地址             地址中的数据

0x08000000   →        0x20005000
                         ↑
                     初始 SP

0x08000004   →        0x08000101
                         ↑
                  Reset_Handler 地址
```

   因此：

```
0x08000004
```
  是：Vector Table 中存放 `Reset_Handler` 地址的 Flash 单元。

---

4. PC 跳转到 `Reset_Handler`

   CPU 得到 `Reset_Handler` 地址以后，会开始从对应位置执行启动代码。

   整个过程可以理解为：

```text
读取：0x08000000
     ↓
获得 SP 初始值
     ↓
SP = 0x20005000


读取：0x08000004
     ↓
获得 Reset_Handler 地址
     ↓
PC 开始执行 Reset_Handler
```

SP 解决的是： **程序运行时 Stack 从哪里开始。**

PC 解决的是： **CPU 接下来从哪里开始执行代码。**

---

5.  `Reset_Handler`

  `Reset_Handler` 是芯片复位以后真正开始执行的启动程序。

  它的核心作用是：**为 C 语言程序正式运行准备好运行环境。**

主要包括：

```text
Reset_Handler
      ↓
初始化运行环境
      ↓
初始化 .data
      ↓
初始化 .bss
      ↓
调用 SystemInit()
      ↓
进入 C 语言运行环境
      ↓
main()
```

---

 6. `.data` 初始化

`.data` 中存放：

```
已经初始化的全局变量
已经初始化的 static 变量
```

例如：

```
int count = 10;
static int state = 3;
```

程序断电之后 SRAM 中的数据会消失，因此这些变量的初始值需要提前保存在 Flash。

启动过程中需要把这些初始值：

```
Flash
  ↓
复制
  ↓
SRAM
```

因此：

```
Flash 中保存初始值
        ↓
芯片启动
        ↓
复制到 SRAM 的 .data
```

这样程序进入 `main()` 后：

```
count == 10
state == 3
```

才能成立。

---

7. `.bss` 初始化

`.bss` (Block Started by Symbol)中主要存放：

```
未初始化的全局变量
未初始化的 static 变量
初始化为 0 的全局 / static 变量
```

例如：

```
int count;
static int state;
```

启动过程中会将这块 SRAM：

```
全部清零
```

因此进入 `main()` 之前：

```
.bss
 ↓
0
```

已经完成。

---

8. `SystemInit()`

启动过程中还会调用：

```
SystemInit();
```

它主要负责进行系统级初始化。

在 STM32 中主要涉及：
```
系统时钟
RCC
PLL
系统运行环境
```

也就是让 MCU 从刚刚复位时的基础状态，进入程序真正需要的运行状态。

可以理解成：
```
刚复位
  ↓
基础运行状态
  ↓
SystemInit()
  ↓
按照程序要求配置系统
```

---

9. 进入 main()

当启动环境准备完成之后，程序最终进入：

```
int main(void)
{
    ...
}
```

从这里开始，才真正进入我们平时编写的用户程序。

因此：`main( )`

并不是芯片上电以后执行的第一段代码。

在它之前已经发生了：

```
Reset
  ↓
启动映射
  ↓
读取初始 SP
  ↓
读取 Reset_Handler
  ↓
执行 Reset_Handler
  ↓
初始化 .data
  ↓
清零 .bss
  ↓
SystemInit()
  ↓
main()
```

## 1.3 核心寄存器家族

在 **ARM Cortex-M 架构（如常用的 STM3 等）** 处理器中，CPU 内核里一共有 **16 个最核心的通用/核心寄存器**（编号为 **R0 到 R15**），每个寄存器都是 32 位长（可以存8 个十六进制数字）

1. 通用数据寄存器：R0 - R12（团队的“搬运工”）

- **R0 - R3**：**参数传递官**。当你在 C 语言里调用一个函数并传参时（比如 `add(a, b)`），编译器会自动把 `a` 和 `b` 的值塞进 R0 和 R1 里送给函数。
- **R4 - R11**：函数如果修改了 R4-R11，按照 ARM 调用约定，一般要负责把原值保存并恢复。CPU 在做复杂的数学运算（如 FreeRTOS 的调度算法、LVGL 计算屏幕坐标）时，中间的临时计算结果会放在这些寄存器里。
- **R12**：**内部脚夫**。属于特殊用途的暂存寄存器。

2. 特殊功能寄存器：R13 - R15（团队的“三大核心大脑”）

   这三个寄存器有自己专属的代号，是控制整个单片机运转的绝对核心：

- **R13 ── SP （Stack Pointer，栈指针）**
    
    - **职责**：盒子里永远存着 **RAM 栈顶的内存地址**。
    - **作用**：**管局部变量和函数保护现场**。FreeRTOS 切换任务时，就是通过修改这个盒子里的地址切换更衣室的。
    
- **R14 ── LR （Link Register，连接寄存器）**
    
    - **职责**：盒子里存着 **函数的返回地址**。
    - **作用**：当 PC 寄存器跳去执行 `delay()` 函数时，LR 会提前记下“等会儿完事了要回 main 函数的哪一行”。没有它，函数跳过去就找不到回家的路了。
    
- **R15 ── PC （Program Counter，程序计数器）**
    
    - **职责**：盒子里永远存着 **下一条要执行的 FLASH 机器指令地址** 。
    - **作用**：系统的绝对指挥棒，通过自动自增和跳转，推着程序一行行往下跑 。
    

---
### 1.3.1 SP (栈指针)
#### 1.3.1.1 基础概念

**SP （Stack Pointer，栈指针）** 和 **PC （Program Counter，程序计数器）** 是 CPU 内部最核心的两个寄存器（可以理解为 CPU 随身携带的超高速、特制微型存储单元）。

不管是 FreeRTOS 做任务切换，还是程序死机，核心都在这两个寄存器上

SP （Stack Pointer，栈指针），**动态数据的“水位调度员”**， 是R13寄存器

- **它是干嘛的**：SP 永远指向 **当前系统“栈（Stack）”内存的最顶端**。
- **工作原理**：栈是专门存局部变量和函数保护现场的。SP 就像是一个 **“刻度尺”或“水位线”**。
    - 当函数里新定义了一个局部变量，数据入栈，SP 的指针位置就会移动（水位上升）。
    - 当函数执行完毕，变量销毁，数据出栈，SP 的指针位置就会移回去（水位下降）。
- **FreeRTOS 的核心秘密**：FreeRTOS 为什么能同时跑好几个任务？因为每个任务都有自己独立的栈。当 FreeRTOS 要从任务 A 切换到任务 B 时，它只需要**把当前 CPU 的 SP 寄存器数值修改成任务 B 的栈顶地址**，CPU 就会瞬间无缝进入任务 B 的世界。这就是多任务操作系统的底层本质。

#### 1.3.1.2 栈排序方式

栈指针是**满递减栈**的

在 ARM Cortex-M 处理器中，SP（寄存器名 R13）管理着 RAM 中的一块区域。ARM 默认采用的是 **“满递减（Full Descending）”** 机制。

- **满（Full）**：SP 指针指向的地址，里面已经装了有效的栈数据。
- **递减（Descending）**：**栈是倒着生长的**。这意味着，你往栈里存的数据越多，SP 里面的内存地址数值反而**越来越小**！

```text
[ RAM 高地址 ] (例如 0x20002000) -> 栈底（刚开机，栈是空的）
      │
      ▼  压栈（Push）：每存入一个数据，SP 的物理地址就减 4 (SP = SP - 4)
[ 栈顶位置 ] (例如 0x20001FDC) -> 此时 SP 寄存器里写的就是这个地址
      ▲
      │  出栈（Pop）：每弹出一个数据，SP 的物理地址就加 4 (SP = SP + 4)
[ RAM 低地址 ] (例如 0x20000000) -> 如果撞到这里，就是堆栈冲突/溢出
```

函数调用时，SP 与 PC 的配合

当程序从 `main()` 跳转到 `delay()` 函数时，CPU 必须做两件事：

1. **保护案发现场**：CPU 必须记住执行完 `delay()` 之后要回到 `main()` 的哪一行（即 `while(1)` 的地址 `0x08000204`）。
2. **配合动作**：
    - **硬件动作**：CPU 将下一条指令地址（返回地址）压入 SP 指向的 RAM 栈中，**SP 自动减小**
    - **软件跳转**：将 `delay()` 的首地址赋给 **PC**，CPU 开始执行新函数。
    - **函数退出**：`delay()` 执行完，CPU 从 SP 指向的栈里把当年存的 `0x08000204` 重新弹出来，直接赋值给 **PC**，程序完美返回 `main()` 继续跑。

---

栈倒着排序，是计算机科学里为了**最大化压榨、节省珍贵的 RAM 内存空间**而演进出的天才设计。
1. 避免资源浪费的“双向奔赴”

   在早期的单片机和计算机里，RAM（运行内存）非常小，往往只有几百个字节。程序运行需要两种动态空间：

```text
Stack
    局部变量
    函数调用现场
    保存寄存器

Heap
    malloc()
    calloc()
    new
    动态内存

.bss
    未初始化 / 初始化为0的全局变量
    static

.data
    已初始化全局变量
    已初始化 static
```

如果让栈和堆都从前往后（从小到大）排队：

- 必须在 RAM 中间画一条死板的**分界线**。
- 结果可能发生：堆里的数据已经满了（爆了），而分界线另一边的栈却空空如也。这就造成了极大的**内存浪费**。

2. 完美的解决方案：对向生长

   于是，天才的架构师决定让它们分别从 RAM 的两头出发，**迎面走来**：

```text
[ 低地址 0x20000000 ] ───────────────────────────────────> [ 高地址 0x20002000 ]

 │  全局/静态变量区  │  堆 (Heap) 🚀 向上生长  │    ...    │  🏁 栈底 (SP初始位置)  │
 └───────────────────┴───────────────────────►           ◄───────────────────────┘
                                                           栈 (Stack) 🔽 向下生长
```

- 把**栈底**设在 RAM 的**最高安全地址**。每当函数调用，SP 指针就**减小**（向下生长）。
- 把**堆底**设在 RAM 的**低地址**。每当申请内存，堆指针就**增大**（向上生长）。
- **好处**：只要“堆”和“栈”的总和没有超过 RAM 的总容量，它们就可以**自由分享、动态压榨中间那一块未知的剩余空间**。只有当它们在中间真正“迎头相撞”时，才会发生堆栈冲突。

### 1.3.2 LR（连接寄存器）

**LR （Link Register，连接寄存器）是 ARM 架构处理器中专门用来存放“函数返回地址”的特殊核心寄存器**。

在 ARM 寄存器战队中，它的物理编号是 **R14**。它与 **PC（程序计数器）**、**SP（栈指针）** 共同组成了控制程序跳转与恢复的“铁三角”

在 C 语言中，CPU 执行代码就像在跑跑步。当遇到函数调用时（例如在 `main` 里面调用 `delay()`），PC 指针必须离开当前跑道，跳去执行 `delay()` 的代码。

**问题来了**：当 `delay()` 的指令全部跑完后，CPU 怎么知道该回到 `main` 函数的哪一行继续往下跑？

- **答案就是靠 LR**。在跳转的一瞬间，硬件会自动把原跑道的“下一行代码地址”记录在 LR 寄存器这个“小本本”上。

用一段真实的 C 语言执行过程，看看这三个寄存器是如何打配合的：
```c
void delay(void) {
  // [位置 B] delay 的业务代码
} // 函数结束，执行汇编指令：BX LR

int main(void) {
    // [位置 A] 
    delay();  // 1. 即将跳转！硬件自动做两件事：
              //    ① 把 [位置 C] 的地址写入 LR 寄存器
              //    ② 把 [位置 B] 的地址写入 PC 寄存器
    while(1); // [位置 C] 
}
```


- **普通的函数调用**：如上图，`delay()` 执行完后，最后一条机器指令固定是 `BX LR`（意思是：把 LR 里的值直接送给 PC）。PC 一变，程序瞬间回到了 **[位置C]**。全过程**不需要读写 RAM 栈（SP）**，效率极高。
- **嵌套调用（套娃）时的 SP 救场**：如果 `delay()` 内部又调用了 `uart_send()`，LR 里的 [位置C] 就会被覆盖。所以，编译器会在进入 `delay()` 的第一秒，生成一条 `PUSH {LR}` 指令，把 LR 的值紧急备份到 **SP 指向的 RAM 栈**里；等函数要结束时，再通过 `POP {PC}` 把值直接弹回 PC。

**CPU 硬件自己是没有未卜先知能力的**。它是瞎子，只管一行行执行机器指令。

判断要不要入栈的，是依靠**编译器（如 Keil、 GCC）**：

- 当你点击编译时，编译器会把你的 C 代码从头到尾扫描一遍。
- 如果编译器发现 `delay()` 内部**又调用了别的函数**，它就会在生成 `delay()` 的机器指令时，雷打不动地在最开头加上一句：`PUSH {R4-R7, LR}`（入栈备份）。
- 如果发现它是最底层的叶子函数，编译器就会优化掉这句指令。一切都是**出厂前（编译期）就由编译器安排好的“剧本”**。

LR 是函数的“返程车票”。单层调用锁在寄存器，嵌套调用备份进堆栈

### 1.3.3 PC (程序计数器)

1. PC （Program Counter，程序计数器）,**CPU 永不停歇的执行引擎**, PC 是个“硬件盒子”（它**永远是 R15 寄存器**），但盒子里装的“FLASH 地址（数字）”每时每刻都在变。

   在 ARM 架构（如 Cortex-M4/M7）中，PC 其实就是芯片内部一组由 32 个电子开关（触发器）组成的 32 位寄存器（名为 R15）。  它里面存放的，永远是一个**绝对的内存地址**（比如 `0x080012A4`）。这个地址指向哪里，CPU 的硬件译码器就会去哪里抓取一条 **ARM 机器指令**

- **它是干嘛的**：PC 永远指向 **当前（或即将）执行的那条 ARM 机器指令的内存地址** 
- **工作原理**：CPU 按照机器指令不断取指、译码和执行，PC 随程序执行向后推进；遇到分支、函数调用、中断等情况时，PC 的执行路线发生改变。 
- **遇到跳转时**：当程序遇到 `if-else`、函数调用，或者 FreeRTOS 切换任务时，PC 的值会被强行修改，直接跳到新函数的地址去执行。
- **死机真相**：当你遇到最让人发怵的死机时，**只要去看一眼此时 PC 寄存器里的十六进制地址**，在反汇编里搜这个地址，就能瞬间抓出具体是哪一行 C 代码引发的崩溃。

2. “取指-译码-执行”的三级流水线

   PC 的自增并不是随意的，它跟随芯片的时钟晶振在疯狂运转。经典的自动自增逻辑如下：

-  **取指（Fetch）**：CPU 根据 PC 指向的 Flash 地址，把那里的 32 位机器码（0和1）读入 CPU 内部。
-  **译码（Decode）**：CPU 内部的硬件电路分析这串机器码，发现它代表的是“加法”还是“跳转”。
-  **执行（Execute）**：执行该指令。与此同时，**PC 自动加上 4 个字节**（32位系统下指令占4字节），指向下一个紧挨着的指令地址。

3. C 语言代码中的 PC 行为

   看看这段 C 代码在运行时，PC 是怎么跳跃的：
```c
void delay(void) {
    int i = 0;       // 假设对应 Flash 地址：0x08000100 (此时 PC = 0x08000100)
}

int main(void) {
                    // 假设对应 Flash 地址：0x08000200
    delay();        // 执行到这里时，PC 被强制修改为 0x08000100（发生跳转！）
    while(1);       // 假设对应 Flash 地址：0x08000204
}
```

## 1.4 中断控制骨架（NVIC）

### 1.4.1 NVIC

NVIC 全称是 Nested Vectored Interrupt Controller，即嵌套向量中断控制器。

它是 ARM Cortex-M 内核中的中断管理模块，负责：

1. 管理中断使能/关闭
2. 管理中断优先级
3. 支持中断嵌套
4. 根据中断向量表跳转到对应 ISR
5. 管理 pending 状态
---
1. NVIC 常用操作

使能中断：

```c
NVIC_EnableIRQ(USART1_IRQn);
```

关闭中断：

```c
NVIC_DisableIRQ(USART1_IRQn);
```

设置优先级：

```c
NVIC_SetPriority(USART1_IRQn, 2);
```

清除挂起：

```c
NVIC_ClearPendingIRQ(USART1_IRQn);
```
---
2. 中断优先级

   在 Cortex-M 中，数值越小，优先级越高。

例如：

```c
NVIC_SetPriority(TIM2_IRQn, 1);
NVIC_SetPriority(USART1_IRQn, 3);
```

则 `TIM2` 优先级高于 `USART1`。如果 USART1 中断正在执行，此时 TIM2 中断来了，TIM2 可以打断 USART1，这就是中断嵌套。

---
3. 抢占优先级和响应优先级

在 STM32 中，中断优先级通常分成：

```text
抢占优先级 Preemption Priority
响应优先级 Sub Priority
```

抢占优先级决定能不能打断别人。响应优先级决定多个同级中断同时 pending 时，谁先执行。

例如：

```text
抢占优先级低数值 = 更高抢占能力
响应优先级低数值 = 同组内更先响应
```

---

4. 中断执行流程

以外部按键中断为例：

```text
按键按下
↓
EXTI 检测到边沿
↓
设置中断 pending
↓
NVIC 判断中断是否使能
↓
NVIC 判断优先级
↓
CPU 自动保存现场
↓
跳转到 EXTI_IRQHandler
↓
执行中断服务函数
↓
清除中断标志位
↓
CPU 恢复现场
↓
回到原来的程序继续执行
```

中断服务函数中一定要清除中断标志，否则可能反复进入中断。

---

5. ISR 编写注意事项

   **中断函数应该尽量短。**

推荐：
```c
void USART1_IRQHandler(void)
{
    if (USART_GetITStatus(USART1, USART_IT_RXNE) != RESET) {
        uint8_t data = USART_ReceiveData(USART1);
        buffer_put(data);
    }
}
```

不推荐在中断里做：

```c
printf();
delay_ms();
while(1);
复杂计算;
大量内存拷贝;
```
中断里应该只做必要工作，把复杂处理放到主循环或任务中。

### 1.4.2 Vector Table（中断向量表）

在计算机科学和数学中，**“向量（Vector）”代表一个有“方向”和“大小”的箭头**。  `Vector Table `是 `Cortex-M` 的异常入口表，同时其前两个表项承担芯片启动所需的` Initial MSP 和 Reset_Handler` 信息。

而在处理器的硬件设计中，这个概念被完美地借用了：

- **方向（目标地址）**：当芯片突发一个事件（比如按键被按下、或者 FreeRTOS 需要切换任务），CPU 的执行路线不能再走直线，它必须发生**转向**，朝着某个特定的函数地址跳过去。
- **中断向量（Interrupt Vector）**：这个用来指引 CPU 转向的“函数绝对物理地址”，就被形象地称为一个 **“中断向量（即引导跳转的指针箭头）”**。
- **中断向量表（Vector Table）**：把芯片所有可能发生的突发事件的引导箭头（向量地址），按照严格的固定顺序排成一个表格，整齐地码放在 Flash 的大门口，这就是 **“中断向量表”** 的由来。

**1个地址装1字节，4个地址装1个字（32位）**。  中断向量表在物理 Flash 芯片上，就是一排雷打不动的 4 字节连续大排档。每一家排档的盒子里，都装满了带奇数 Thumb 标志的 32 位函数绝对地址

```text
【 高地址 ── 向量表的后排房间 】
  │
  ├── 🏠 0x08000060 ── 内部装有 0x08005A1D (USART1 串口中断函数地址) -> 接收 OTA 数据靠它
  ├── 🏠 0x0800005C ── 内部装有 0x08004B31 (EXTI0 外部引脚中断函数地址) -> 检测按键按下靠它
  │
  │   [ ... 中间排着大大小小几十个芯片厂家做好的外设硬件中断通道 ... ]
  │
  ├── 🏠 0x0800003C ── 内部装有 0x08001F45 (SysTick_Handler 心跳地址) -> FreeRTOS 切任务的亲爹
  ├── 🏠 0x0800000C ── 内部装有 0x080012C1 (HardFault_Handler 死机地址) -> 程序崩溃时的终点站
  ├── 🏠 0x08000004 ── 内部装有 0x08000101 (Reset_Handler 复位函数地址) -> 开机 PC 闪现的第一步
  ├── 🏠 0x08000000 ── 内部装有 0x20005000 (初始 SP 的值) ─── 🏁 整个芯片启动的总源头！
  │
【 低地址 ── 统一编址总起点 】
```

### 1.4.3 ISR

ISR 全称：Interrupt Service Routine中文一般叫：**中断服务程序 / 中断服务例程**

```c
void USART1_IRQHandler(void)
{
    ...
}
```

这个 `USART1_IRQHandler()` 本质上就是一个 ISR。也就是说：IRQ = Interrupt Request中断请求  ISR = Interrupt Service Routine 真正处理中断的函数

可以理解成：硬件发生事件 - 产生 IRQ - NVIC 接收到 IRQ - CPU 查 Vector Table - 找到对应 Handler 地址 - 执行 ISR

例如串口：USART1 收到数据 - RXNE = 1 - 产生 USART1_IRQn - NVIC - Vector Table - USART1_IRQHandler()

所以严格来说：USART1_IRQHandler()是函数名；

而从功能角色上讲，它就是：USART1 的 ISR

---

ISR 需要注意。

1. **ISR 是被硬件事件触发的，不是主程序正常顺序调用的**。
2. ISR要**快进快出**。因此一般不要在里面
```text
delay_ms(1000);
printf大量内容;
while长时间等待;
复杂算法;
```
更好的方式是：
```c
void USART1_IRQHandler(void)
{
    data = USART_ReceiveData(USART1);
    
    flag = 1;
}

//然后：
while(1)
{
    if(flag)
    {
        flag = 0;
        // 真正的大量处理
    }

}
```

```text
或者 RTOS 里面：ISR - Queue / Semaphore - Task - 真正处理
```

### 1.4.4 SCB

SCB 全称：System Control Block，中文：**系统控制块**。它不是 STM32 普通外设，而是： **ARM Cortex-M 内核自带的一组系统控制寄存器。**

你可以这样理解 STM32：
```text
STM32
│
├── Cortex-M4 内核
│     ├── R0~R15
│     ├── NVIC
│     ├── SysTick
│     └── SCB
│
└── ST 外设
      ├── GPIO
      ├── USART
      ├── TIM
      ├── DMA
      └── ADC
```

所以：USART1->DR 是 STM32 外设寄存器。

但是：SCB->VTOR 属于： ARM Cortex-M4 内核控制寄存器。

---

SCB 具体管什么

SCB 相当于： **CPU 内核的系统管理控制台。**

常见成员包括：
```text
VTOR
AIRCR
ICSR
SHCSR
CFSR
HFSR
```

`SCB->VTOR = 0x08010000;`意思： 告诉 Cortex-M：以后发生异常/中断时，去 `0x08010000` 开始找向量表。

这里：
```text
SCB
↓
系统控制块
  

VTOR
↓
SCB里面的一个寄存器

所以不要把：SCB 和：VTOR当成同一级别。
```

### 1.4.5 VTOR（向量表偏移寄存器）

**VTOR** 的全称是 **Vector Table Offset Register（向量表偏移寄存器）**。如果把**中断向量表**比作 CPU 桌上的《紧急突发事件客服通讯录》，那么 **VTOR 寄存器就是控制这本通讯录摆放位置的“物理底座”**

在普通的单片机程序中，芯片内部只有一套代码，中断向量表默认死死固化在物理 FLASH 的大门口（`0x08000000`）。CPU 只要发生任何中断，都会雷打不动地去这里查表。

但当引入了 **Bootloader** 和 **App（主程序）** 的双段固件设计（比如要做 **OTA 远程升级**）时，致命的冲突就来了：

1. **Bootloader** 住在 `0x08000000`，它有一张属于自己的中断向量表。
2. **App（跑 FreeRTOS 和 LVGL 的主程序）** 搬家到了新基地址 `0x08010000`，它也自带了一张全新的中断向量表。

当 Bootloader 顺利跳转到 App 运行后，用户突然点了一下触摸屏（触发外部中断）。由于 CPU 内部的中断控制器（NVIC）默认是个瞎子，**它如果没有接到新的圣旨，发生中断时依然会本能地闪现回老家 `0x08000000` 去查旧表！**

- **后果**：CPU 会读出 Bootloader 的中断函数地址，导致程序在 App 运行期间莫名其妙跳回了 Bootloader 甚至直接死机。

**为了解决这个冲突，VTOR 寄存器横空出世。** 它的唯一使命，就是允许我们在程序运行期间，**用一句代码强行改变 CPU 查找中断向量表的物理基地址**。

真实应用场景

1. 开机阶段（VTOR 默认为 0）

   芯片刚通电，VTOR 寄存器里面的数值默认是 `0x00000000`。  
   因为硬件重映射的存在，此时零地址就是 `0x08000000`。CPU 乖乖地去 Bootloader 的老家查表、启动。

2. 跳转阶段（修改 VTOR，大权移交）

   Bootloader 执行完汇编跳转代码，控制权交给了 App 。  
   App 活过来的第一件事（通常在 `main()` 函数的第一行，或者启动文件调用的 `SystemInit()` 函数里），必须雷打不动地写下这句系统级代码：

```c
// 强行把 App 的新家基地址写进内核的 VTOR 寄存器
SCB->VTOR = 0x08010000; 
```

3. 产生变化的物理本质

   当这行代码执行完，**VTOR 寄存器这个硬件盒子里的数字瞬间变成了 `0x08010000`**。  
   这时候，中断控制器（NVIC）内部的硬件物理连线被直接更改。

- 过了 1 毫秒，**FreeRTOS 的心跳（SysTick 中断）** 爆发了。
- CPU 再次擦擦眼睛：“收到！现在 VTOR 告诉我基地搬了，我直接去 **`0x08010000` 往后的新大排档**里查表，读取 App 自带的 `SysTick_Handler` 函数地址！”
- 于是，FreeRTOS 的多任务调度和 LVGL 的触摸刷新终于在新的领地上完美、顺畅地跑起来了，彻底和老家的 Bootloader 划清了界限。

### 1.4.6 项目需要两个不同的中断向量表

**Bootloader 和 App 是两个完全独立编译、在不同时间运行、功能完全无关的独立程序，它们的函数在 Flash 里的物理房间号（地址）完全不同！**

如果公用一张表会发生什么？

假设我们在整个 Flash 里**只在 `0x08000000` 留一张中断向量表**，让 Bootloader 和 App 共用。

有一天，系统正在运行 **App（比如跑 FreeRTOS 和 LVGL 刷新界面）**，突然产生了一个 **SysTick（心跳）中断**。

1. CPU 收到中断，本能地闪现回老家 `0x08000000` 去查表。
2. 此时，表里的 `SysTick_Handler` 房间里应该填哪一个函数的地址？
    - **选项 A**：填 Bootloader 里的心跳函数地址。
    - **选项 B**：填 App 里的心跳函数地址。

- **如果填了 A （Bootloader 的函数）**：App 运行期间一发生心跳，程序瞬间就会**穿越回 Bootloader 去了**！LVGL 界面当场卡死。
- **如果填了 B （App 的函数）**：App 确实能正常跑了。但是！当芯片刚开机，还在跑 **Bootloader 进行固件校验或擦写 Flash** 的时候，也突然来了一个中断。CPU 查表，读出了 App 的函数地址（此时 App 甚至还没下载完，是一片空地 `0xFFFFFFFF`），PC 指针一跳，**当场死机变砖**！

在电脑前开发时，**Bootloader 是一个 Keil 工程，App 是另一个完全独立的 Keil 工程**。

- **Bootloader 编译时**：它根本不知道你未来的 App 长什么样、有多大、里面的函数会被分到哪个具体的物理地址（比如 `0x0810452C`）。所以它在自己的大门口（`0x08000000`）构建了一张专属于自己的“通讯录”，里面的电话号码全是 Bootloader 内部函数的。
- **App 编译时**：它也只管自己的世界。编译器把它的 FreeRTOS、 LVGL 逻辑打包，在它的大门口（`0x08100000`）同样构建了一张全新的“通讯录”，里面的电话号码全部指向 App 内部高频蹦迪的那些函数。

它们在物理上被你隔开成了两个完全合法的“独立小王国”。

## 1.5 机器指令与反汇编基础

### 1.5.1 函数地址 bit0 与 Thumb 状态

在 ARM 架构（如 STM32 等单片机）中，诸如 `0x080000A1` 这样的奇数地址，代表的是 **带有“Thumb 状态标志”的函数或指令跳转目标**

末尾的数字 `1` 并不是指真正的物理房间号，它只是编译器给 CPU 硬件贴的一张“小纸条”，用来强行下令：“接下来的代码必须用 16 位的 Thumb 模式（精简指令集）来运行“

32 位 ARM 处理器中，不管是 32 位的 ARM 指令还是 16 位的 Thumb 指令，它们在内存里的首地址**都必须是偶数（能被 2 或 4 整除）**。这意味着，任何有效指令的物理地址，其二进制的最后一位（第 0 位）**注定永远是 0**。

当 C 语言里调用一个函数，或者在中断向量表里看到复位地址写着 `0x080000A1` 时：

1. **剥离标志位**：CPU 内部的硬件电路在真正开船去 Flash 搬数据之前，会一巴掌把最后一位的 `1` 自动抹掉，还原成真正的偶数物理房间号 **`0x080000A0`**。
2. **抓取指令**：CPU 依然是从 `0x080000A0` 开始打包抓取指令。
3. **切换状态**：但因为刚才那个 `1` 的存在，CPU 内部的状态寄存器（EPSR 的 T 位）会被瞬间强行写为 1，CPU 明白接下来要按 Thumb 指令的规矩来解码。

### 1.5.2 汇编语言

所有的 ARM 汇编语言，其核心格式都是一个雷打不动的“三段式”：

==操作码 (Opcode)    目标寄存器(Destination)    源操作数(Source)==

经典动作演示：数据的读取与存入

数据平时住在 **RAM/Flash（0x20/0x08）** 里，但 CPU 要计算它们，必须先用大触手抓进 **CPU 内部的核心寄存器（R0~R12）**。这个过程靠两条指令：

- **`LDR` (Load Register) ── 从内存房间开箱取货**

```assembly
LDR R0, [R1]  ; 含义：去 R1 寄存器里写的那个内存地址，把里面的数据读出来，塞进 R0 寄存器
```

- **`STR` (Store Register) ── 把货存进内存仓库**
  
```assembly
STR R0, [R1]  ; 含义：把 R0 寄存器里的计算结果，强行写入到 R1 里存放的那个内存地址中
```
   
2. 经典动作演示：寄存器之间的倒手与计算

- **`MOV` (Move) ── 内部复制**

```assembly
MOV R0, R1    ; 含义：把 R1 寄存器里的值，复制一份给 R0 寄存器
```
  
- **`ADD` (Add) ── 硬件加法器起动**
```assembly
ADD R0, R1, R2 ; 含义：把 R1 和 R2 里的数值相加，结果倒进 R0 里面
```
### 1.5.3 符号理解

1. 中括号`[ ]`
   中括号 `[ ]` 在汇编里被亲切地称为 **“寻址指针符号”** ，它相当于 C 语言里的**指针取值符** `*`
   
   核心对比：不加中括号 vs 加中括号

   我们通过最核心的 `LDR`（读取）和 `MOV`（倒手）指令，看看加不加中括号对 CPU 硬件电路产生的巨大物理变化：

- 不加中括号：只在 CPU 内部“倒腾盒子”

```assembly
MOV R0, R1    ; 含义：把 R1 寄存器里的数字，直接复制一份塞进 R0 寄存器
```

   **物理现状**：CPU 根本不需要派大触手去外面的内存条（RAM）或者闪存（Flash）里找货。电信号仅仅是在 CPU 核心内部的 R1 盒子和 R0 盒子之间流过，速度极快（通常只需要 1 个时钟周期）。

- 加了中括号：必须出动总线触手“去内存开箱取货”
```assembly
LDR R0, [R1]  ; 含义：把 R1 里的数字看作是一个“内存房间号”（地址），去那个房间把货读出来，塞进 R0
```

   **物理现状**：这时候，CPU 内部的物理 R1 盒子里装的是比如 `0x20001000`。因为加了 `[ ]`，硬件电路瞬间被激活：地址总线将 `0x20001000` 发送给外面的 **SRAM 存储芯片**，从那个物理房间里抓出真正的局部变量数据，一路小跑运回 CPU 塞进 R0。
   
   ---

 2. 井号 `#`
 
  在 ARM 汇编语言中，**井号 `#` 代表“立即数（Immediate Number）”**。大白话来说，**`#` 就是用来告诉你后面跟着的数字是一个“纯粹的、死板的数学常数”**，而不是寄存器编号，也不是内存房间号

  CPU 是个极其死板的硬件。如果不加 `#`，CPU 根本分不清你给它的数字到底是**“常数”**还是**“寄存器号”**。

我们来看一组经典的对比：

- **不加 `#`（误当成寄存器）**：
 ```assembly
MOV R0, 1    ; ❌ 错误写法！
 ```
 
  CPU 的内心戏_：“你让我把 1 复制给 R0？这个 1 是指 **R1 寄存器** 吗？还是指什么硬件外设？我糊涂了，不执行！”
  
- **加了 `#`（明确是常数）**：
```assembly
MOV R0, #1   ;  正确写法！
```
  CPU 的内心戏_：“哦！看到了 `#`，说明后面的 1 就是数学里的数字 1。我直接把数字 1 塞进 R0 寄存器盒子里！”（相当于 C 语言的 `a = 1;`）

---

3. 融合
   `LDR R0, [R1, #4]` ➡️ 有 `[]` 和 `#`：去**【房号 R1 + 4 个单间】**的那个新内存房间里拿货 。（C语言：`a = *(b + 1);` // 假设b是int指针）

   计算机的宏观地图规定了**“低地址在最底下，高地址在最上面”**（地基是 `0x00000000`，顶天是 `0xFFFFFFFF`）。

在执行 `LDR R0, [R1, #4]` 这条汇编指令时，底层的物理运算其实是一个非常简单的**硬件加法**：

- **获取基地址**：假设 `R1` 寄存器盒子里写着的房号是 `0x20000000` 。
- **加上立即数**：硬件加法器收到 `#4` 这个常数命令，直接让地址做加法：`0x20000000 + 4 = 0x20000004`。
- **寻找新房间**：因为数字从 `0x20000000` 变大到了 `0x20000004`，在宏观地图上，指针的轨迹自然是**直直地“向上拔高”了 4 个字节的距离**！
---
### 1.5.4 汇编指令速查

核心语法公式

```assembly
操作码 (Opcode)  目标寄存器 (Dest) ,  源操作数 / 地址 (Src)
```

一、 内存传输家族

由于 ARM 属于 RISC（精简指令集）架构，**CPU 不能直接对内存（RAM/Flash）里的数据进行数学运算**。必须先用大触手抓进寄存器，算完再放回去。

1. `LDR` (Load Register) ── 从内存开箱取货

- **语法**：`LDR R0, [R1]`
- **大白话**：去 `R1` 寄存器里写的那个内存地址，把里面的数据读出来，塞进 `R0` 寄存器。
- **变种（带偏移）**：`LDR R2, [R0, #4]` ➡️ 去 `R0+4` 的内存地址（比如**中断向量表的 4 号房**）取货，存入 `R2` [Confounding Sources]。
- **实战联想**：Bootloader 去 App 基地址摸出初始 SP 和复位 PC 必用此指令 

2. `STR` (Store Register) ── 把货存进内存仓库

- **语法**：`STR R0, [R1]`
- **大白话**：把 `R0` 寄存器里的计算结果，强行写入到 `R1` 里存放的那个内存地址中。
- **实战联想**：你在 C 语言里修改一个全局变量，或者往 `0x40000000` 区间控制外设引脚电平时，底层全在疯狂调用 `STR`。

---

二、 寄存器倒手家族（CPU 内部的高速传送带）

3. `MOV` (Move) ── 寄存器间复制

- **语法**：`MOV R0, R1`
- **大白话**：把 `R1` 寄存器里的值，复制一份给 `R0` 寄存器。
- **常数赋值**：`MOV R0, #5` ➡️ 直接在 `R0` 盒子里写下数字 5（相当于 C 语言 `int a = 5;`）。

4. `MSR` / `MRS` (Move to/from Special Register) ── 操控特殊寄存器

- **语法**：`MSR MSP, R1`
- **大白话**：把 `R1` 寄存器里的值，强行写入到 **MSP（物理主堆栈指针寄存器 SP）** 里面。
- **实战联想**：**Bootloader 强改更衣室水位线的核心神技！**

---

三、 方向盘转弯家族（强扭 PC 指针实现跳转）

5. `B` (Branch) ── 跳转

   B 无链接分支 
   BL 带链接分支 
   BX 寄存器间接分支/状态相关跳转

- **语法**：`B Reset_Handler`
- **大白话**：强制让 **PC 寄存器** 直接变成复位函数的地址，程序发生跳跃。
- **实战联想**：死机时启动文件里的 `B .`（原地打转死循环）就是它。

6. `BL` (Branch with Link) ── 函数调用（买好回程车票）

- **语法**：`BL delay`
- **大白话**：强扭 PC 跳去跑 `delay` 函数。但在跳跃前的**同一纳秒，自动把当前下一行地址写进 LR (R14) 寄存器里**，当作回程车票。
- **实战联想**：C 语言里所有的标准函数调用，底层全被编译器翻译成 `BL`。

7. `BX` (Branch and Exchange) ── 带状态跳转（查验车票）

- **语法**：`BX LR` 或 `BX R2` 
- **大白话**：把 `LR` 或 `R2` 里的数字强行倒给 **PC 寄存器**。同时，它会自动检查这个地址的最后一位是不是 `1` 。如果是 `1`，就锁死 CPU 的 Thumb 运行状态 。
- **实战联想**：函数退出返回、或者 Bootloader 最后一脚把 PC 踢进 App 时必用 。

---

四、 弹簧收纳桶家族（操控 SP 栈指针高频快进快出）

8. `PUSH` ── 压入栈内存（SP 变小）

- **语法**：`PUSH {R4, LR}`
- **大白话**：把 `R4` 和 `LR` 寄存器里的值，塞进 **SP 指向的 RAM 栈顶**。由于栈是满递减倒着长的，**SP 的物理地址会自动减小（水位线被压低）**。
- **实战联想**：函数套娃嵌套、或者突发中断需要保护案发现场时使用。

9. `POP` ── 弹出栈内存（SP 变大）

- **语法**：`POP {R4, PC}`
- **大白话**：从 RAM 栈顶把数据摸出来放回 `R4`。最绝的是，它顺手把当年存的返回地址**直接弹进了 PC 寄存器**！PC 一变，函数瞬间完成了原路返回，同时 **SP 地址自动增大（水位线退回）**。

---

五、 数据运算家族（硬件加法器与逻辑门）

10. `ADD` / `SUB` ── 加法与减法

- **语法**：`ADD R0, R1, R2` ➡️ 算出 `R1 + R2` 的值，倒进 `R0` 里面。
- **语法**：`SUB R0, R0, #1` ➡️ 让 `R0` 里的数字自减 1（相当于 `i--`）。

11. `CMP` (Compare) ── 硬件死磕对比

- **语法**：`CMP R0, #0`
- **大白话**：拿 `R0` 寄存器里的数字跟 0 做减法对比。它不改变 R0 的值，但如果减出来的结果是 0，就会把 CPU 的 **Z 状态标志位** 勾选上。后面紧跟一条 `BEQ`（等于就跳转）或 `BNE`（不等于就跳转），这就完美实现了 C 语言里的 `if-else` 分支。

### 1.5.5 双下划线`__`的理解

在 C 语言和嵌入式开发（如你的 STM32 标准库、FreeRTOS 底层代码）中，在函数名、变量名或宏定义前面看到的双下划线 `__`（比如 `__set_MSP()`、`__disable_irq()`），代表的是==**“编译器或硬件内核专属的底层保留字”**==。

一句话直接说破本质：**双下划线是在警告普通开发者：“这是芯片内核或编译器在幕后调用的‘秘密武器’，它是直接和底层硬件或汇编指令挂钩的，没事千万不要随便去修改它！”**

1. 它是编译器官方的“私人领地标志”

   根据 C 语言的国际标准（ANSI C / ISO C）规定：**所有以双下划线 `__` 开头的标识符，都属于编译器（如 Keil 的 ARMCC/ARMCLANG、GCC）和芯片核心库的“专属保留字”**。

- **为什么这么霸道？**：为了防止“重名冲突”。假设你做项目时，自己写了一个普通的延时函数叫 `void delay(void);`。如果芯片官方的底层库里也写了一个内部延时函数叫 `void delay(void);`，两个工程一链接，编译器就会当场因为“命名冲突”而罢工。
- **怎么解决？**：官方为了不和我们普通程序员写的普通函数撞车，就会在自己所有的底层核心函数前，全部整整齐齐地焊上 `__`。这样，官方的 `__set_MSP` 就永远不可能和我们写的函数重名。

2. 它是底层“内联汇编”的化身（硬件直通车）

   在 C 语言里调用普通的函数（比如 `lv_obj_create()`），CPU 在底层需要大费周章地执行 `BL` 指令去买回程车票（压栈、跳转）。  
   但在嵌入式里，有些动作必须**一纳秒都不能耽误，甚至需要直接调用一条特殊的 ARM 汇编指令**。这时候，双下划线函数就上场了，它们在技术上叫做 **“编译器内建函数 (Built-in / Intrinsic Functions)”**：

- 我们之前在 Bootloader 跳转里见过的：
 ```c
__set_MSP(sp); 
 ```

 `__`它的真身_：它表面上是一个 C 语言函数，但当你点击编译后，编译器会一巴掌把它的外壳拍碎，**直接原地把它替换成一条纯硬件的 ARM 汇编指令**：`MSR MSP, R0`。中间没有任何函数调用的开销，直达硬件寄存器！
- 再比如你在跳转前必须执行的关闭总中断函数：

```c
__disable_irq(); 
```

`_`它的真身`_`：编译后，原地直接变成一条汇编：`CPSID I`（硬件级关闭所有可屏蔽中断）。

3. 常见的双下划线“特种兵”速查

- **`__inline` 或 `__inline__`**：内联函数标志。告诉编译器：“这个函数很小，调用它时不要让我频繁跳进跳出了，直接把它的汇编代码复制粘贴到调用它的地方，用空间换绝对的速度！”
- **`__weak`**：**弱定义标志**。我们在启动文件里聊过，它告诉编译器：“我先在底层写一个默认的死循环函数。如果用户未来在 C 语言里写了一个同名的真函数，你就把我这个弱定义的函数无情覆盖掉！”
- **`__packed`**：取消对齐标志。如果在结构体前面加上 `__packed`，就是强行下令：“编译器听好了，收回你的强迫症！把中间的空房间全部砸掉，把变量死死压缩黏在一起，就算 CPU 抓取变慢，我也要把空间省到极致！”

### 1.5.6 C语言高级规范

#### 1.5.6.1 常见命名规则

1. `_t` ── **Type (数据类型模板标志)**

```c
typedef void (*app_func_t)(void); // 👈 后面加了 _t
```

- **代表什么**：`_t` 意思是 **Type（类型）**。
- **物理本质**：它告诉你，**这个名字在内存里目前没有占 1 个比特的空间**！它不是一个可以装货的真实实体盒子，它只是一个 **“模具 / 模板”**。
- **为什么要加**：提醒不能直接拿它来用。必须用这个模板去“生”一个真变量。
    - _错误写法_：`app_func_t = 0x08100000;` ❌（不能往一个塑料模具里倒水）。
    - _正确写法_：`app_func_t my_jump = (app_func_t)pc;` （用模具生出了一个叫 `my_jump` 的真实指针盒子，它在栈里占了 4 个字节，可以尽情操控它！） [Confounding Sources]。


2. `_Handler` ── **中断服务程序专属外号 (Interrupt Handler)**

   在**中断向量表**和**启动文件（**`.s`**）** 里频繁看到它：  
   `Reset_Handler`、`HardFault_Handler`、`SysTick_Handler`。

- **代表什么**：`Handler` 意思是 **“处理者 / 接线员”**。
- **物理本质**：这是一个**极其特殊的函数地址**。它绝对不允许在普通的 C 语言代码里像普通函数那样随随便便去调用它（比如你不能在 main 里写 `HardFault_Handler();`）。
- **为什么要加**：专门留给 **NVIC 中断控制器**硬件的大触手查阅的。一旦物理引脚爆发警报、或者 FreeRTOS 心跳响了，CPU 会通过中断向量表，直奔这个带有 `_Handler` 后缀的机器指令大门口，强制 PC 指针闪现进去执行紧急救援。

---

3. `_p` 或 `p` 开头 ── **Pointer (指针箱子)**

   在写 **Ring Buffer 环形队列**的解码代码时看过它：

```c
uint8_t *p_data = buffer; // 👈 变量名前面加了 p
```

- **代表什么**：`p` 意思是 **Pointer（指针）**。
- **物理本质**：大声警告你：“这个盒子（变量）里装的**绝对不是普通的数学数字（如电量、步数）**，这里头装的是一个活生生的、32位的 **RAM / Flash 房间门牌号（地址）**！” 
- **为什么要加**：防止你扣错字。看到带 `p` 的变量，你就知道如果要拿里面的货，必须给它套上我们刚学透的**中括号 `[]` 汇编逻辑（在C语言里对应解引用符 `*p_data`）**，让总线触手去那个房间里开箱取货。

---

4. `_Handler_t` 或 `_Handle` ── **外设大管家指针 (Peripheral Handle)**

```
UART_HandleTypeDef huart1; // 👈 后面加了 Handle
```

- **代表什么**：`Handle` 意思是 **“把手 / 句柄”**。
- **物理本质**：这叫外设的“方向盘”。因为串口 1 拥有密密麻麻几十个控制引脚和时钟寄存器（全都堆在 `0x40000000` 区间）。如果你定义了 `huart1`，它就像是一张**全权委托书的指针**。
- **为什么要加**：以后你要控制串口 1 发送数据，你不需要记任何寄存器地址，直接把这个句柄抓过来丢给官方函数：`HAL_UART_Transmit(&huart1, ...)`。函数顺着这个把手，就能一秒摸到串口 1 在 `0x40000000` 处的物理控制台。
---
#### 1.5.6.2 数字后缀

`#define FW_MAGIC 0x5AA55AA5U`

这个：U  代表： **Unsigned，无符号。** 也就是说：100U,告诉编译器：这个整数常量按照 unsigned 类型处理。

例如：10   默认通常首先按照：int理解。而：10U,明确表示：unsigned int

嵌入式里大量涉及：
```text
寄存器
地址
位运算
长度
Mask
Magic Number
```

这些东西通常：**没有负数意义。** 例如：`#define BUFFER_SIZE 4096U` 缓冲区不可能：-4096 Byte所以用 unsigned 很合理。再比如：`#define USART_TIMEOUT 1000000U`Timeout 不应该是负数。

---
与位运算相关

例如：`1 << 31`,在一些类型规则下可能引入有符号整数相关的问题。因此工程里经常写：`1U << 31`

意思：先把1明确当成unsigned-- 再左移31位。这对于寄存器位 Mask 特别常见。

例如：
`#define BIT31 (1U << 31)`

# 2 片上外设

**“片上外设”是单片机微控制器（MCU）除了 CPU 核心之外，集成在同一块硅片上的所有硬件功能模块的统称。**

如果将 **CPU 内核**（包含 PC、SP 等核心寄存器）比作整个系统的**“大脑”** ，那么片上外设就是大脑延伸出来的 **“四肢、神经与五官”** 。单纯的 CPU 只能在内部孤立地进行二进制数学运算，只有通过片上外设，芯片才真正具备了与外部物理世界进行电信号连接、时序控制和数据交互的能力。

## 2.1 引脚控制家族

（芯片的“触手”）

1.GPIO (通用输入输出)

- **物理本质**：芯片外围物理引脚的直接控制器。
- **核心功能**：输入模式下，读取引脚上的物理高低电平（检测按键、传感器状态）；输出模式下，控制引脚输出高低电平（点亮 LED、控制继电器开关）。它是所有复杂外设物理信号的最终出口。

## 2.2 物流与时序家族

（芯片的“血脉与心脏”）

2.DMA (直接内存访问)

- **物理本质**：独立于 CPU 的纯硬件高速数据搬运工。
- **核心功能**：在不需要 CPU 干预的情况下，直接在“内存与外设”或“内存与内存”之间拉起一条硬件管道搬运海量物资，是实现系统异步非阻塞运行的物理基石。

3.RCC / CRG (时钟控制与复位)

- **物理本质**：芯片内部的“脉搏和心脏总管”。
- **核心功能**：负责管理芯片内部所有物理晶振的频率，并为各个片上外设分发“时钟信号（Clock）”。在嵌入式中，外设如果不开启时钟，其内部的晶体管就不会导通，外设就处于绝对的物理断电/睡眠状态。

## 2.3 数据通信家族
（芯片的“外交官”）

通信外设负责把芯片内部的并行数字信号，转化为特定协议的串行物理电平向外发送。

4.UART / USART (通用异步/同步收发器)

- **物理本质**：最基础、最通用的串行通信外设。
- **核心功能**：通常只需要 2 根线（TX 发送，RX 接收）。由于没有时钟线，双方必须物理约定好相同的“波特率（Baud Rate）”。常用于连接 GPS 模块、蓝牙模块、4G 模块以及电脑调试窗口（Printf）。

5.SPI (串行外设接口)

- **物理本质**：高速、全双工、同步串行总线。
- **核心功能**：通常需要 4 根线（时钟 CLK、主出从入 MOSI、主入从出 MISO、片选 CS）。由单片机提供无情的物理时钟脉冲，传输速率极高（可达几十 MHz）。**它是驱动你正在研究的 W25Q128 Flash 芯片以及各种彩屏（LCD/OLED）的绝对主力**。

6.I2C / IIC (集成电路总线)

- **物理本质**：双线制、半双工、同步串行总线。
- **核心功能**：只需要 2 根线（时钟 SCL、数据 SDA）。它拥有极其美妙的“器件寻址机制”，一根总线上可以并联挂载上百个不同的片外器件。常用于读取 MPU6050 陀螺仪、EEPROM 存储器、以及小型 OLED 屏幕。

7.CAN (控制器局域网络)

- **物理本质**：汽车与工业级的差分总线。
- **核心功能**：利用两根线的“电平差值”来传输信号，物理抗干扰能力极强。它是汽车车载电子（如发动机控制、车窗、雷达）和大型工业自动化设备的绝对标准。

## 2.4 定时与控制家族（芯片的“时间刻度”）

8.TIM (定时器/计数器)

- **物理本质**：由芯片内部时钟脉冲驱动的精密硬件计数器。
- **核心功能**：
    - **基本定时**：每隔精准的微秒级时间，触发一次内核中断（比如用于跑操作系统的滴答时钟）。
    - **PWM 输出**：通过让引脚高低电平交替跳变，输出脉冲宽度调制信号，用来**物理控制舵机角度、电机转速、或者 LED 的呼吸灯亮度**。
    - **输入捕获**：测量外部物理引脚上电平跳变的时间差，常用于测量超声波测距模块的回波时间或编码器的测速。

9.WDG (看门狗定时器)

- **物理本质**：系统死机物理复位器。
- **核心功能**：它是一个独立的递减计数器。程序正常运行时，App 层必须定期去重置这个计数器（俗称“喂狗”）。如果系统由于屎山代码、死循环或强电磁干扰导致死机，无法及时喂狗，看门狗数到 0 的瞬间会**物理强制向内核发送复位信号，让单片机瞬间重新热启动**，是工业级产品高可靠性的护身符。

## 2.5 模拟/数字转换家族（芯片的“五官”）

CPU 内核只能认识 0 和 1 的数字世界，而物理世界（温度、电压、声音）是连续的模拟量。

10.ADC (模数转换器)

- **物理本质**：物理电压测量仪。
- **核心功能**：把物理引脚上感受到的连续模拟电压值（如 1.25V），精准测量并转换为单片机看得懂的数字（如 12 位 ADC 会将其转换为 1551）。常用于采集电瓶电量、光敏电阻、温度传感器数据。

11.DAC (数模转换器)

- **物理本质**：可控电压发生器。
- **核心功能**：与 ADC 相反，你给它一个数字，它控制物理引脚输出对应的模拟电压值。常用于音频信号解码输出、或者控制工业变频器。

## 2.6 高级网络与存储家族
（芯片的“大武器”）

在稍微高端一点的 MCU（如 STM32F4/H7、ESP32）中，还会集成以下硬核外设：

12.FMC / FSMC (灵活静态存储控制器)

- **物理本质**：外部并行总线扩展器。
- **核心功能**：可以直接把外部的 SDRAM（内存条）、NAND Flash、或者 8080 接口的大彩屏，物理映射到单片机内核的寻址空间里。让 CPU 可以像读写内部 SRAM 一样，用一条 C 语言指针赋值语句直接去刷新屏幕像素或读写外部内存。

13.ETH (以太网 MAC 控制器) / Wi-Fi & BT (无线基带)

- **物理本质**：网络物理层/数据链路层硬件。
- **核心功能**：在芯片内集成了处理网络数据包、无线射频信号的硬件状态机。配合片上的加密外设（AES/DES），让单片机具备直接接入互联网、进行物联网通信的能力。
 ---
# 3 片外外设
## 3.1 LCD屏幕

### 3.1.1 简介

TFT-LCD 即薄膜晶体管液晶显示器。其英文全称为:Thin Film Transistor-Liquid Crystal Display。TFT-LCD与无源TN-LCD、STN-LCD的简单矩阵不同,它在液晶显示屏的每一个象素上都设置有一个薄膜晶体管(TFT),可有效地克服非选通时的串扰,使显示液晶屏的静态特性与扫描线数无关。

TFT-LCD具有:亮度好、对比度高、层次感强、颜色鲜艳等特点。应用于电视、手机、电脑、平板等各种电子产品。

分辨率:240+320驱动IC: ILI9341

自带触摸屏（电阻触摸屏)16位80并口驱动

16位真彩显示(65536色)

### 3.1.2 读写数据

| 引脚名称    | 代码变量名     | 功能描述      |
| :-------: | :---------: | :---------: |
| NCS     | CS        | 片选信号      |
| DNC     | RS        | 数据/命令选择信号 |
| NRD     | RD        | 读信号       |
| NWR     | WR        | 写信号       |
| DB7-DB0 | DATA_PORT | 数据总线      |

1. CS（Chip Select）作用：片选用于 SPI 或并口通信。

   当：CS = 0 → TFT被选中

   CS = 1  → TFT不响应

   如果总线上有多个设备，就需要 CS

2. DC / RS（Data Command）不同模块叫法不同：

DC （Data/Command）

RS （Register Select）

作用：DC = 0 → 发送命令   DC = 1 → 发送数据

例如：写寄存器地址 → DC=0   写像素数据 → DC=1

这是非常重要的控制线

## 3.2 HC-05 模块

### 3.2.1 简介

- HC-05和HC-06是广泛使用的**经典蓝牙模块**，常用于与单片机（如Arduino）进行无线通信。
- 它们基于蓝牙2.0标准，支持串口通信（UART）协议，使得与单片机的连接和数据交换相对简单。
- HC-05可作为主设备或从设备，支持蓝牙SPP（串口通信）和AT命令模式。
- HC-06通常作为从设备，仅支持蓝牙SPP模式。
- 它们具有基本的通信范围，通常在10米左右。

### 3.2.2 HC-05参数

|名称|说明|
|:---:|:---:|
|接口说明|TTL，兼容3.3V/5V单片机系统|
|波特率|4800.9600（默认）、19200、38400、57600、115200、230400、460800、921600、1382400|
|工作温度|-25°C~75°C|
|工作电压|DC3.3V-5.0V|
|工作电流|配对中：30~40mA，配对完毕未通信：1-8mA；通信中：5-20mA|

### 3.2.3 HC-05工作模式

具有两种工作模式：命令响应工作模式和自动连接工作模式，在自动连接工作模式下模块又可分为主（Master）、从（Slave）和回环（Loopback）三种工作角色。

当模块处于**自动连接工作模式**时，将自动根据事先设定的方式连接的数据传输；

当模块处于**命令响应工作模式**时能执行下述所有AT命令，用户可向模块发送各种AT指令，为模块设定控制参数或发布控制命令。通过控制模块外部引脚（PIO11）输入电平，可以实现模块工作状态的动态转换。

1. Master（主角色）—查询周围SPP蓝牙从设备，并主动发起连接，从而建立主、从蓝牙设备间的透明数据传输通道。
2. Slave（从角色）—被动连接；
3. Slave-Loop（回环角色）—被动连接，接收远程蓝牙主设备数据并将数据原样返回给远程蓝牙主设备；

### 3.2.4 引脚定义

1. PIO8 连接LED，指示模块工作状态，模块上电后闪烁，不同的状态闪烁间隔不同。当灯快闪的时候，就是自动连接工作模式；当灯慢闪的时候，就是命令响应工作模式。
2. PIO9连接LED，指示模块连接成功，蓝牙串口匹配连接成功后，LED 长亮
3. PIO11 模块状态切换脚，高电平-->AT 命令响应工作状态，低电平或悬空-->蓝牙常规工作状态。
4. 模块上已带有复位电路，重新上电即完成复位。

### 3.2.5 AT指令模式

- 按下模块上的按键（PIO11置高）
- 对HC-05重新上电，模块进入AT命令响应工作模式。
- 使用串口助手，配置成波特率38400，数据位8位，停止位1位，无校验位，无流控制。串口助手发送字符串“AT\r\n”，正常情况下模块会给出“OK\r\n”，**"\r\n”为回车换行，也叫做新行**。

### 3.2.6 常见指令
|    分类     |    AT 命令    |           功能描述            |
| :-------: | :---------: | :-----------------------: |
| **基础与测试** |     AT      |     测试串口通信，正常返回 `OK`      |
|           |  AT+RESET   |           模块重启            |
|           | AT+VERSION? | `ATE1` 开启回显 / `ATE0` 关闭回显 |
|           |   AT+ORGL   |        查看固件/软件版本信息        |
|           |  AT+ADDR?   |         获取蓝牙模块地址          |
| **设备与硬件** |  AT+NAME?   |       查询设备 IMEI 串号        |
|           |  AT+ROLE?   |     查询 SIM 卡 IMSI 号码      |
|           |  AT+PSWD?   |   检查 SIM 卡状态（如是否锁定或就绪）    |
| **网络与信号** |  AT+UART?   |        查询信号强度和误码率         |
|           |  AT+BIND?   |     查询网络注册状态（是否找网成功）      |
|           |  AT+STATE?  |       查询当前连接的运营商名称        |

---
# 4 数据缓冲与数据流管理 
## 4.1 Buffer 基础 

假设 UART 收数据。对方可能：

```
每隔几十微秒
 ↓
送来 1 Byte
```

但是你的 CPU 可能正在：

```text
刷新 LCD
运行 FreeRTOS
计算 CRC
写 Flash
处理业务逻辑
```

如果每收到一个 Byte，就要求业务程序马上完整处理：

```text
UART
 ↓
收到数据
 ↓
CPU赶紧处理
 ↓
又收到数据
 ↓
CPU再处理
```

程序会非常被动。所以更好的思想是： **先收下来，以后再处理。**

于是：

```
UART
 ↓
Buffer
 ↓
业务程序慢慢读取
```

Buffer 就像： **临时仓库。** 
## 4.2 Ring Buffer

普通的Buffer，例如：

```c
uint8_t buffer[8];
```

最开始：

```
[ ][ ][ ][ ][ ][ ][ ][ ]
 ↑
从这里写
```

不断收到数据：

```
[A][B][C][D][E][ ][ ][ ]
```

继续：

```
[A][B][C][D][E][F][G][H]
```

现在数组尾部到了。

但是前面的：

```
A B C D
```

可能已经被程序读取完了。

理论上前面已经空出来：

```
[ ][ ][ ][ ][E][F][G][H]
```

可普通线性 Buffer 的写指针已经到了数组末尾。怎么办？一种笨方法：把 `E F G H` 全部整体搬到数组前面。这样非常浪费 CPU。

---

Ring Buffer：

 **把一个普通数组的头和尾在逻辑上接起来。**

例如：

```
        ┌───────────────┐
        ↓               │

[0][1][2][3][4][5][6][7]
 ↑                      │
 └──────────────────────┘
```

当写指针到：

```
7
```

以后：

```
下一个位置
 ↓
0
```

所以它像一个环。

### 4.2.1 Ring Buffer 中两个最重要的指针

通常有：

```c
head / write
```

写指针。

以及：

```c
tail / read
```

读指针。

例如：

```
Index:

 0   1   2   3   4   5   6   7

[A] [B] [C] [D] [ ] [ ] [ ] [ ]
                 ↑
                head

 ↑
tail
```

含义：

```
tail
 ↓
下一个应该读取的位置

head
 ↓
下一个应该写入的位置
```

### 4.2.2 写入数据

收到：

```
E
```

写到：

```c
buffer[head]
```

然后：

```c
head++
```

变成：

```
[A][B][C][D][E][ ][ ][ ]
                   ↑
                  head
```

如果：

```c
head == BUFFER_SIZE
```

那么：

```c
head = 0
```

重新绕回数组开头。

常见写法：

```c
head = (head + 1) % BUFFER_SIZE;
```

也就是：

```
0 → 1 → 2 → 3 → ... → 7 → 0 → 1 ...
```

---

### 4.2.3 读取数据

程序读取：

```c
buffer[tail]
```

然后：

```c
tail++
```

例如：

```
[A][B][C][D][E][ ][ ][ ]
 ↑
tail
```

读取 A：

```
 A 被消费
    ↓
[A][B][C][D][E][ ][ ][ ]
    ↑
   tail
```

注意： **通常并不需要真的把 A 从 RAM 中清零。**

只要：

```
tail
```

移动了，就意味着： “A 这个位置已经没有有效数据了。”以后新的数据可以覆盖它。
### 4.2.4 环形到底发生在哪里？

假设 8 Byte Ring Buffer：

```
index:
0 1 2 3 4 5 6 7
```

现在：

```
head = 7
```

写一个数据以后：

```
head
 ↓
0
```

于是：

```
6 → 7 → 0 → 1
```

从物理 RAM 上看：

```
buffer[7]
```

和：

```
buffer[0]
```

根本没有真的连在一起。

实际上还是普通数组：

```
0 1 2 3 4 5 6 7
```

所谓“Ring”： **只是软件通过 head/tail 的回绕逻辑，把它当成一个环。** 这个理解很重要。

### 4.2.5 Ring Buffer 更高效

普通线性 Buffer 用完前面空间后，可能需要：

```
整体搬数据
```

Ring Buffer：

```
数据不搬
 ↓
只移动 head / tail
```

也就是： **数据尽量不动，指针动。** 这就是它非常适合嵌入式的原因。因为：

```
修改 head
```

可能只是修改一个整数。

而搬运几 KB 数据，则需要 CPU 做大量内存读写。
### 4.2.6 Ring Buffer + UART

这是最经典的使用方式。

```
UART硬件
   ↓
收到 Byte
   ↓
UART ISR
   ↓
放进 Ring Buffer
   ↓
head 移动
   ↓
中断快速退出

与此同时

main / Task
   ↓
检查 Ring Buffer
   ↓
根据 tail 取数据
   ↓
解析 Data Packet
```

因此：

```
UART负责收
Ring Buffer负责存
Parser负责解析
业务程序负责处理
```

职责非常清晰。

### 4.2.7 Ring Buffer + DMA

再往后学 DMA 时，关系可以继续升级：

```
UART
 ↓
DMA
 ↓
Buffer / Ring Buffer
 ↓
CPU
 ↓
协议解析
```

这样 CPU 连：

```
每个 Byte 都进入一次中断
```

都不一定需要。这也是以后做高吞吐 UART、OTA 时非常重要的一条路线。

---
# 5 通信协议与数据组织 
## 5.1 数据帧 

### 5.1.1 概念

假设 UART 不断收到：

```
AA 01 03 11 22 33 7E AA 02 02 44 55 91 ...
```

UART 本身只知道：“我不断收到一个 Byte、一个 Byte、一个 Byte……”

它不知道：

```text
哪里是一条消息的开始？

哪里是一条消息的结束？

这条消息多长？

数据是什么意思？

数据有没有损坏？
```

所以程序员需要自己规定：**一组字节按照什么格式排列。**这就是： **数据帧 / 数据包。**

### 5.1.2 典型数据包

一个典型数据包

例如我们自己设计：

```text
┌──────┬──────┬──────┬──────────────┬──────┐
│ HEAD │ TYPE │ LEN  │     DATA     │ CRC  │
├──────┼──────┼──────┼──────────────┼──────┤
│ 2B   │ 1B   │ 2B   │   N Byte     │ 2B   │
└──────┴──────┴──────┴──────────────┴──────┘
```

假设实际收到：

```text
AA 55  01  00 03  11 22 33  8A 7C
```

可以解析为：

```text
AA 55
  ↓
帧头 HEAD

01
 ↓
TYPE

00 03
 ↓
Length = 3

11 22 33
   ↓
Payload

8A 7C
  ↓
CRC
```

于是 CPU 就知道： “我收到了一条完整的消息，它是 Type=1，里面有 3 Byte 数据。”

### 5.1.3 数据包各部分作用

1. HEAD —— 帧头

例如：

```
AA 55
```

作用： **告诉接收方：一条新数据包开始了。**

如果 Ring Buffer 中有很多乱码：

```
43 71 92 AA 55 01 03 ...
         ↑ ↑
```

程序可以不断寻找：

```
AA 55
```

找到以后：“这里可能是一条新数据包的开头。”

---

2. TYPE / CMD —— 类型或命令

例如：

```text
01 = 查询版本
02 = 固件数据
03 = 开始升级
04 = 升级完成
```

作用： **告诉接收方这条消息要干什么。** 这和后面的 TLV `Type` 思想非常相似。

---

3. LEN —— 长度

告诉接收方： **Payload 有多少 Byte。**

例如：

```c
LEN = 0x0003
```

那么：

```
接下来读取 3 Byte
```

就是：

```
11 22 33
```

---

4. DATA / Payload

这是真正有效的数据。

例如 OTA：

```
DATA
 ↓
256 Byte 固件数据
```

例如传感器：

```
DATA
 ↓
温度、湿度
```

---

5. CRC / Checksum

用来检查： **传输过程中数据有没有发生错误。**

例如原来：

```
11 22 33
```

结果通信干扰变成：

```
11 26 33
```

CRC 校验就可能失败。

于是接收方：

```
CRC错误
 ↓
丢弃
或者
要求重新发送
```

---

6. 数据包与TLV的关系 

这个非常重要。它们不是同一个东西。可以：

```text
一个完整数据包

HEAD
TYPE
LEN
PAYLOAD
CRC
```

其中：

```text
PAYLOAD
```

内部再采用：

```text
TLV
TLV
TLV
```

例如：

```
┌───────────────────────────────┐
│          Data Packet          │
│                               │
│ HEAD                          │
│ TYPE                          │
│ LEN                           │
│                               │
│ PAYLOAD                       │
│   ├─ TLV 01                   │
│   ├─ TLV 02                   │
│   └─ TLV 03                   │
│                               │
│ CRC                           │
└───────────────────────────────┘
```

所以： **数据包规定“整条消息长什么样”。** 而：**TLV 规定“消息里面的数据怎么组织”。**

### 5.1.4 Magic Header

1. 基础知识

   Magic Header 是**放在一段数据最前面的固定特殊数值**，用来告诉程序：“这是什么数据”。

   例如设计一个升级固件：

```text
地址                内容

0x08020000          0x4D484657

0x08020004          固件长度

0x08020008          固件CRC

0x0802000C          固件版本

0x08020010          真正固件数据...
```

  其中：

   0x4D484657

  如果我们规定：

```c
#define FW_MAGIC 0x4D484657
```

   那么它就是： **Firmware Magic Number**Bootloader 读到这个值，就知道：“这里确实像是一个我认识的固件。”

  它其实就是：`0x4D484657`,这样一个普通数字。“Magic”的意思只是：大家提前约定了一个非常特殊、不容易碰巧出现的值。

Bootloader 检查：

```c
if (header.magic == FW_MAGIC)
{
    // 固件格式看起来正确
}

如果不是：

else
{
    // 不认为这里是合法固件
}
```

---
2. 与普通通信数据帧之间的关系

   普通的通信数据帧是：
```
HEAD | TYPE | LEN | DATA | CRC
```

   例如：
```
AA 55 | 01 | 0003 | 11 22 33 | CRC
```

  这里：

  AA 55

  就是一种非常简单的 **Magic Header 思想**。

  作用：
```text
收到一堆字节
     ↓
寻找 AA 55
     ↓
找到
     ↓
可能是一条合法数据包
     ↓
继续解析 LEN / DATA / CRC
```

  而固件 Magic Header：
```
Flash里一堆数据
     ↓
读取开头4字节
     ↓
是不是 FW_MAGIC？
     ↓
是
     ↓
按照固件格式继续解析

```

所以逻辑其实完全一样。

---
3. Magic与Magic Header
 
   Magic Header ≠ 完整 Header,**Magic Number**只是：0x5AA55AA5一个固定值。**Header**则是一整个结构：
```c
typedef struct
{
    uint32_t magic;
    
    uint32_t firmware_size;

    uint32_t firmware_crc;

    uint32_t version;

} firmware_header_t;

```

  这里：整个结构体 = Firmware Header,其中 magic = Magic Number所以严格来说： **Magic Number 是 Header 里面的一个字段。** 不过工程里有时候也会口语化说： Magic Header指“以 Magic 开头的固件头”。

## 5.2 TLV 协议 

**TLV（Type-Length-Value，类型-长度-值）是嵌入式系统、物联网（IoT）通信协议、固件打包以及数据存储中最经典、最通用且最高效的一种“结构化数据打包协议”**。

如果把变量和数据比作要通过网络发送或存入 Flash 的**“货物”**：

- **普通结构体打包**：是定制的“死格子货车”**。每个格子的尺寸和位置出厂就焊死了。一旦格子不够大，或者货物品种变了，整辆车就得报废。
- **TLV 打包**：是“标准集装箱” 。不管里面的货物是什么，每一个集装箱前面都雷打不动地贴着三张标签：**“这是啥（Type）”**、**“有多长（Length）”**、**“具体货物（Value）”**。

在物理存储（如 RAM/Flash 连续单间）或网络数据包里，一组 TLV 数据就是一串连续排队的**字节链条**：

| 字段名称         | 专业术语             | 占用字节数（典型配置）          | 它的物理使命                                           |
| :---: | :---: | :---: | :---: |
| **T ── 类型**  | **Type** (或 Tag) | 通常固定占 **1 或 2 字节**   | 告诉接收方（CPU）接下来的数据代表什么含义。                          |
| **L ── 长度**  | **Length**       | 通常固定占 **1、2 或 4 字节** | 精准划定后面 `Value` 的**物理字节边界**，告诉 CPU 应该往后打包吸走多少个房间。 |
| **V ── 实际值** | **Value**        | **动态可变**（由 L 的数值决定）  | 真正要传递的数据核心。可以是数字、字符串、结构体、甚至嵌套另一组 TLV。            |

有了 TLV，我们根本不需要改动任何数据格式，直接像连环画一样把数据往下贴即可。  
我们可以提前约定：`Type = 0x01` 代表电量，`Type = 0x02` 代表步数，`Type = 0x03` 代表心率。

1.0 版本的旧设备发送的数据包（十六进制字节串）：

`01 04 00 00 00 64` `02 04 00 00 03 E8`

- 拆解看：
    - `01 04 00 00 00 64` ➡️ 类型是 01（电量），长度是 4 字节，值是 `0x64`（十进制 100% 电量）。
    - `02 04 00 00 03 E8` ➡️ 类型是 02（步数），长度是 4 字节，值是 `0x03E8`（十进制 1000 步）。

2.0 版本的新设备，直接在屁股后面加一节火车车厢：

`01 04 ... 02 04 ... 03 04 00 00 00 4B`

- 拆解看：最后面直接多贴了一组 `03 04 00 00 00 4B` ➡️ 类型是 03（心率），长度 4 字节，值是 `0x4B`（十进制 75 次/分）。

🏆 奇迹发生了：

- **新服务器收到老手表的数据**：发现没有 `Type=0x03`，知道这是老设备，直接不读取心率， battery 和 steps 依旧能**对齐、完美识别**！
- **老服务器收到新手表的数据**：当它顺着字节从小到大抓取时，读完 01 和 02 之后，突然摸到了 `Type=0x03`。老服务器翻遍字典发现自己不认识 03。但由于有 **L（长度=4字节）** 的存在，老服务器高冷地下令：“我不认识这个货，但既然写着长度是 4，**大触手直接帮我往后跳过 4 个物理房间**，去看看后面还有没有别的货！”
- **结果**：新旧设备、新旧服务器完美共存，业务绝不翻车！这就是强大的**动态动态协议容错性**。

## 5.3 TLV 解析 

假设我们要打包两个数据：① 传感器 ID 为 `0x05`；② 当前温度为 `26℃`（十六进制为 `0x1A`）。  
如果直接传物理值 `05 1A`，接收端根本无法区分谁是谁。用 TLV 编码后：

- **数据1（ID）**：`01 01 05` （Type=`01`代表ID，Length=`01`占1字节，Value=`05`）
- **数据2（温度）**：`02 01 1A` （Type=`02`代表温度，Length=`01`占1字节，Value=`1A`）
- **最终在总线（如串口）上流动的 Packet**：`01 01 05 02 01 1A`

在嵌入式中，由于内存不能随便 `malloc`，我们通常在串口中断或 DMA 接收到一串缓存后，使用**状态机**或**流式指针**自底向上遍历解析。

```c
// 1. 定义我们认识的 Type 字典（宏定义）
#define TYPE_DEVICE_ID   0x01
#define TYPE_TEMPERATURE 0x02
#define TYPE_HUMIDITY    0x03

// 2. 模拟从串口接收到的一串混合新老版本的 TLV 原始数据包
uint8_t rx_packet[] = {0x01, 0x01, 0x0A, 0x99, 0x01, 0xFF, 0x02, 0x01, 0x1A}; 
// 说明：
// [01 01 0A] -> 设备 ID 为 10
// [99 01 FF] -> 这是一个未来新版本加的、当前版本不认识的 Type 0x99，长 1 字节
// [02 01 1A] -> 温度为 26

void TLV_Parse(uint8_t *buffer, uint16_t length) {
    uint16_t index = 0;

    // 当指针没有超过整个 Packet 的安全边界时，循环解析
    while (index < length) {
        uint8_t type   = buffer[index++]; // 提取 Type，指针挪 1 步
        uint8_t len    = buffer[index++]; // 提取 Length，指针挪 1 步
        
        // 安全边界检查：防止 Length 字段损坏导致指针越界
        if (index + len > length) {
            printf("Error: TLV Length out of bounds!\n");
            break;
        }

        uint8_t *value = &buffer[index];  // 静态分配思想：用指针直接指向原数组的 Value 区域，无需复制

        // 根据不同的 Type 走进不同的业务
        switch (type) {
            case TYPE_DEVICE_ID:
                printf("SUCCESS: Device ID parsed: %d\n", *value);
                break;
                
            case TYPE_TEMPERATURE:
                printf("SUCCESS: Temperature parsed: %d C\n", *value);
                break;

            default:
                // 重点：遇到不认识的 Type（如 0x99），不报错，不挂起，优雅跳过！
                printf("WARNING: Unknown Type [0x%02X], auto-skipping %d bytes...\n", type, len);
                break;
        }

        index += len; // 核心：指针向后跨过当前 Value 的长度，准备解析下一个 TLV 单元
    }
}

int main() {
    TLV_Parse(rx_packet, sizeof(rx_packet));
    return 0;
}
```
# 6 嵌入式软件架构基础

## 6.1 耦合与解耦

**耦合 Coupling** 可以简单理解为： 两个模块之间“绑得有多死”。

**解耦 Decoupling**： 尽量让不同模块少知道彼此的内部细节，通过清晰接口进行合作。

如果不解耦对于后续函数修改会很麻烦

```c
void TIM2_IRQHandler(void)

{
    if (TIM_GetITStatus(TIM2, TIM_IT_Update))
    {
        TIM_ClearITPendingBit(TIM2, TIM_IT_Update);  
        LED_Toggle();
    }
}

```

现在：`TIM Driver` 直接知道：LED,于是：TIM模块 - 绑死 - LED模块。如果以后我不要闪 LED，而要：发送UART，得直接修改：`TIM2_IRQHandler()`变成：`UART_Send();`TIM 模块和业务逻辑绑得很死。这就叫：**高耦合。**

----

通过使用回调函数实现解耦合

针对同样的TIM中断案例

```c
typedef void (*time_elapsed_callback_t)(void);
  
static time_elapsed_callback_t callback;
  
void time_elapsed_register(time_elapsed_callback_t cb)
{
    callback = cb;
}
```

TIM 中断只负责：
```c
if (callback)
{
    callback();
}
```

TIM 模块根本不知道 callback 最终干什么。可以：`time_elapsed_register(LED_Toggle);`也可以：`time_elapsed_register(send_uart);`甚至：`time_elapsed_register(sensor_sample);`

于是变成：TIM Driver - 统一callback接口 - 应用层自己决定干什么

这就是： **解耦。**

---

解耦合真正的作用

# 7 中间件层
## 7.1 LVGL

LVGL （Light and Versatile Graphics Library，轻量级通用型图形库）是目前全球在嵌入式和微控制器（MCU/MPU）领域中最流行、最活跃的免费开源 GUI（图形用户界面）库

1. 核心特性
- **极其丰富的内置组件（Widgets）**：内置了 30 多个标准组件，包括**按钮（Button）、滑块（Slider）、图表（Chart）、日历（Calendar）、下拉列表、键盘**等，全部支持动态创建和销毁。]
- **现代化的视觉特效**：原生支持高级图形效果，如**抗锯齿（边缘平滑）、高级动画、透明度叠加、平滑滚动、圆角阴影和渐变色**。 
- **Web 级的布局机制**：引入了类似网页前端的 **Flexbox（弹性盒子）和 Grid（网格）布局管理器**，当屏幕分辨率发生改变时，界面能够自动做响应式适配。]
- **强大的样式系统**：采用类似于网页 **CSS 样式表** 的设计思想。你可以给一组按钮统一定义一种“皮肤样式”，当进入按下（Pressed）或禁用（Disabled）状态时，样式会自动切换。]
- **极佳的商用协议**：采用 **MIT 开源协议**，意味着你完全可以用它来开发商业闭源产品，不需要支付任何版权费。

2. 它对单片机的硬件要求高吗？（低资源消耗）

LVGL 是纯 C 语言编写的（完美兼容 C++），它没有任何外部库依赖，可以运行在**纯裸机**或者任何 **RTOS（如` FreeRTOS、RT-Thread`）** 上。

想要跑一套精美的 UI，它的**硬件物理门槛最低只需要**： 

- **Flash（闪存）**：约 `200KB - 300KB`（用于存放核心库代码与基础字体）。
- **RAM（运行内存）**：约 `100KB`。
- **渲染缓冲区**：极度节省内存！它甚至**不需要一整块屏幕大小的帧缓冲区**。你只需要开辟一个相当于 **1/10 屏幕大小**的行缓冲区，利用 DMA 异步刷新，就能跑到 60 帧的丝滑效果。

3. LVGL 编程的“三大支柱”思想

- **对象系统（Object System）**：一切皆对象。在代码中，基础的基础是一个叫 `lv_obj_t` 的结构体指针。按钮是对象，标签是对象，屏幕本身也是一个大对象。它们通过**父子层级关系**嵌套组合在一起。
- **样式系统（Style System）**：通过 `lv_style_t` 结构体来描述对象的颜色、边宽、圆角等外观。通过将样式“绑定”到对象上，实现业务逻辑与美工视觉的分离。
- **事件系统（Event System）**：采用类似高级语言的“信号与槽”或回调函数机制。通过 `lv_obj_add_event_cb` 给按钮绑定一个你写好的函数指针。当用户物理触摸点击屏幕、或者转动旋钮时，单片机会自动触发该回调函数去执行你的应用代码。

## 7.2  Free RTOS

RTOS （Real Time Operating System，中文就是实时操作系统）

FreeRTOS是一个迷你的实时操作系统内核。作为一个轻量级的操作系统，功能包括：任务管理、时间管理、信号量、消息队列、内存管理、记录功能、软件定时器、协程等，可基本满足较小系统的需要。

由于RTOS需占用一定的系统资源（尤其是RAM资源），只有uC/OS-I、口embOS、salvo、FreeRTOS等少数实时操作系统能在小RAM单片机上运行。相对uC/OS-II、embOS等商业操作系统，FreeRTOS操作系统是完全免费的操條3585462系统，具有源码公开、可移植、可裁减、调度策略灵活的特点，可以方便地移植到各种单片机上运行。

FreeRTOS的设计小巧且简易，整个核心代码只有3到4个C文件，为了让代码容易阅读、移植和维护，大部分的代码都是以C语言编写，只有一些函数（多数是架构特定排班副程序）采用汇编语言编写。

### 7.2.1  FreeRTOS移植

1、添加RTOS源码到Keil工程

2、添加head_4.c到Keil工程

3、添加port.c到Keil工程

4、添加头文件路径

5、添加FreeRTOSConfig.h

6、修改FreeRTOSConfig.h配置文件，直到工程编译无错误

### 7.2.2 创建任务Task/Thread

```c
BaseType_t xTaskCreate( TaskFunction_t pxTaskCode,

                        Const char * const pcName,

                        Const configSTACK_DEPTH_TYPE usStackDepth,

                        Void * const pvParameters,

                        UBaseType_t uxPriority,

                        TaskHandle_t * const pxCreatedTask)
```

pxTaskCode:指向任务函数的指针，注意，任务函数不能返回（即死循环）

pcName：任务名，字符串

usStackDepth：栈深，即任务的栈大小（单位是字，1字 = 4字节）

pvParameters：任务的参数指针（即FreeRTOS 允许你给任务函数传一个“通用指针”）

uxPriority：任务的优先级，最低优先级是0，数字越大，优先级越高

pxCreatedTask：任务的句柄，用于控制任务

在调用的时候使用

```c
xTaskCreate(led_blink, "led_blink", 256, (void*)&LED0, 1, NULL);

xTaskCreate(led_blink, "led_blink", 256, (void*)&LED1, 1, NULL);

xTaskCreate(led_blink, "led_blink", 256, (void*)&LED2, 1, NULL);
```

进行调用，注意到第四个参数是(void*)&LED0

&LED0表示：取 LED0 这个结构体变量的地址。

因为 LED0 是一个结构体变量：

```c
LED_TypeDef LED0 =
{
	GPIOB, GPIO_Pin_0, RCC_AHB1Periph_GPIOB
};
```

那么：&LED0类型就是：LED_TypeDef* 也就是“指向 LED_TypeDef 的指针”。(void*)&LED0表示：把 LED_TypeDef* 转换成 void*，传给 FreeRTOS。因为 xTaskCreate 第四个参数规定就是 void*。void* 可以理解为：通用地址类型，什么类型的地址都可以先放进来。然后到了任务函数里面，你再转换回来：

```c
LED_TypeDef *led = (LED_TypeDef *)args;
```

|             函数             | 含义      | 返回值     |
| :------------------------: | :-----: | :-----: |
|      `xTaskCreate()`       | 创建任务    | 有，成功或失败 |
|       `vTaskDelay()`       | 当前任务延时  | 无       |
|  `vTaskStartScheduler()`   | 启动调度器   | 无       |
| `xTaskGetSchedulerState()` | 获取调度器状态 | 有       |
|      `vTaskDelete()`       | 删除任务    | 无       |

xTask 和 vTask 不是不同任务，而是 FreeRTOS 的函数命名习惯；v 通常表示无返回值，x 通常表示有返回值，Task 表示任务管理相关函数

### 7.2.3 队列

#### 7.2.3.1 创建队列Queue

```c
QueueHandle_t xQueueCreate(UBaseType_t uxQueueLength,

                           UBaseType_t uxItemSize);  
```

`xQueueCreat` 函数有两个参数`uxQueueLength`和 `uxItemSize`

`uxQueueLength`：队列能够存储的最大消息数目，即队列长度

`uxItemSize`：队列中消息的大小，一字节为单位

返回值：如果创建成功则返回一个队列句柄（就是队列结构体的地址），用于访问创建的队列

如果创建不成功则返回NULL，可能原因是创建队列需要的RAM无法分配成功。

| 内容          | 任务 Task          | 队列 Queue      |
| :-----------:| :----------------: | :-------------: |
| 本质          | 一段独立运行的代码        | 一个数据缓冲区       |
| 是否会被 CPU 执行 | 会                | 不会            |
| 作用          | 执行功能             | 传递数据          |
| 例子          | 读取传感器、BLE发送、控制电机 | 传颜色、传传感器值、传命令 |
| 由谁使用        | 调度器调度任务运行        | 任务之间读写队列      |

相当于创建了一个大数组

#### 7.2.3.2  队列发送函数
```c
BaseType_t xQueueSend(QueueHandle_t xQueue,

                      const void * pvItemToQueue,

                      TickType_t xTicksToWait);
```

xQueue：要写入的队列

pvItemToQueue：要写入的消息（数据的地址）

xTicksToWait：阻塞超时时间（当队列为满，是否需要进行阻塞等待）

返回值：

1.pdTRUE：写入成功

2.errQUEUE_FULL：队列满，写入失败

#### 7.2.3.3  队列接收函数
```c
BaseType_t xQueueReceive(QueueHandle_t xQueue,

                         const void * pvBuffer,

                         TickType_t xTicksToWait);
```

xQueue：要写入的队列

pvBuffer：要写入的消息（数据的地址）

xTicksToWait：阻塞超时时间（当队列为空，是否需要进行阻塞等待）

返回值:

1.pdTRUE：写入成功

2.errQUEUE_FULL：队列为空，写入失败

#### 7.2.3.4 队列发送/接收函数中断版本

```c
BaseType_t xQueueSendFromISR (QueueHandle_t xQueue,

                             const void * pvItemToQueue,

                             BaseType_t * pvHigherPriorityTaskWoken);  

BaseType_t xQueueReceiveFromISR (QueueHandle_t xQueue,

                                 const void * pvBuffer,

                                 BaseType_t * pvHigherPriorityTaskWoken);  
```


中断中是不能进入阻塞态

中断中不能立马切换任务

中断是快进快出，执行的代码越少越好

### 7.2.4 信号量Semaphore

信号量 Semaphore 本质上是 RTOS 里用来做同步和资源控制的机制。信号量不是用来传具体数据的，而是用来告诉任务：“某件事发生了” 或 “某个资源现在可以用了”

我们希望任务都是互斥的，同一个时间段一个任务只能被一个人调用

在多任务里，每个任务一定是顺序执行的，他们各自独立，以不可预知的速度向前推进，但有时候我们希望多个任务能密切合作以实现一个共同的任务。绒布就是在多任务的一些关键点上可能需要互相等待和互通消息

Task = 人

Queue = 快递柜，用来放数据

Semaphore = 门铃，用来通知有人可以行动

Mutex （互斥）= 厕所门锁，同一时间只能一个人用

Handle = 钥匙 / 编号 / 地址

Scheduler = 管理员，决定谁先执行
